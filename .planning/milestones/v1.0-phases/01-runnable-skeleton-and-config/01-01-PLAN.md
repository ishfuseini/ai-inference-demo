---
phase: 01-runnable-skeleton-and-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SETUP-01]

estimate:
  tokens: 4000
  raw_tokens: 4000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "SETUP-01 package assumption resolved: the exact PyPI package names `nicegui`, `httpx`, `langfuse`, `pytest`, and `ruff` are verified before `uv sync` resolves dependencies."
    - "D-11 remains intact: the planned dependency stack is approved before any dependency-resolution task runs."
  artifacts: []
  key_links:
    - from: ".planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md"
      to: ".planning/phases/01-runnable-skeleton-and-config/01-02-PLAN.md"
      via: "Package Legitimacy Audit gate must precede dependency resolution"
      pattern: "Package Legitimacy Audit"
  prohibitions:
    - "No package-manager install or lockfile generation before this gate is approved."
    - "No replacement dependency stack that violates D-11."
---

<objective>
Resolve the Phase 1 package-legitimacy gate before dependency installation.

Purpose: The research audit marked all planned PyPI packages as `SUS`, while D-11 locks the stack. This plan gets explicit human verification before Plan 01-02 writes dependency metadata and runs `uv sync`.
Output: Approval signal for `nicegui`, `httpx`, `langfuse`, `pytest`, and `ruff` as the Phase 1 package set.
</objective>

<execution_context>
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Phase 1 PyPI package legitimacy before dependency resolution</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @.planning/REQUIREMENTS.md
  </read_first>
  <what-built>No source files are changed in this gate. The executor confirms the registry identities for the D-11 package set before Plan 01-02 can create `pyproject.toml` and `uv.lock`.</what-built>
  <how-to-verify>
    1. Review the PyPI pages for `nicegui`, `httpx`, `langfuse`, `pytest`, and `ruff`.
    2. Confirm they are the intended packages for NiceGUI, HTTPX, Langfuse Python SDK, pytest, and Ruff.
    3. Confirm there is no `[SLOP]` package in `.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md`.
    4. Type `approved` to allow Plan 01-02 dependency resolution, or describe the package concern.
  </how-to-verify>
  <action>Use the Package Legitimacy Audit in `01-RESEARCH.md` to gate D-11 exactly as written: `nicegui`, `httpx`, `langfuse`, `pytest`, and `ruff` remain the approved packages unless the user rejects one at this checkpoint.</action>
  <verify>
    <automated>python3 -m pip index versions nicegui && python3 -m pip index versions httpx && python3 -m pip index versions langfuse && python3 -m pip index versions pytest && python3 -m pip index versions ruff</automated>
    <human-check>Human confirms the registry identities and replies `approved`.</human-check>
  </verify>
  <acceptance_criteria>
    - The checkpoint records a human `approved` signal before any `uv sync`, `uv lock`, or dependency write task runs.
    - The approved package set still satisfies D-11 and SETUP-01.
    - If the user rejects a package, execution stops and returns a blocking checkpoint instead of substituting a dependency.
  </acceptance_criteria>
  <done>Human approval is captured for the exact Phase 1 package set, unblocking Plan 01-02.</done>
  <resume-signal>Type `approved` or describe package concerns.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer to PyPI registry | Dependency names and versions cross from public package registry into local execution. |
| planner to executor | Research audit decisions become executable dependency-resolution instructions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-SC | Tampering | PyPI dependency selection | high | mitigate | Blocking package-legitimacy checkpoint before Plan 01-02 dependency resolution for all `[SUS]` packages. |
| T-01-01-01 | Spoofing | Package registry identity | medium | mitigate | Human verifies package pages and intended project ownership before approving the package set. |
| T-01-01-02 | Repudiation | Package approval trail | low | mitigate | Executor records the checkpoint result in `01-01-SUMMARY.md`. |
</threat_model>

<verification>
Plan 01-01 succeeds only after the blocking package checkpoint receives an `approved` signal. No source or dependency files are written by this plan.
</verification>

<success_criteria>
SETUP-01 can proceed with reviewed package names, and D-11 is not altered by the executor.
</success_criteria>

<output>
Create `.planning/phases/01-runnable-skeleton-and-config/01-01-SUMMARY.md` when done.
</output>
