---
phase: 02-streaming-inference-evidence
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app.py
  - src/openrouter_demo/ui.py
  - tests/test_ui.py
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05, INF-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "INF-01: `Run Inference` is enabled for non-empty prompts when `OPENROUTER_API_KEY` is configured and disabled when the key is missing or a request is in flight."
    - "INF-02: the response panel shows `Streaming from OpenRouter...` while streaming and appends response text progressively."
    - "INF-03: completed run UI shows selected strategy `Default` and actual model/provider evidence when available."
    - "INF-04: telemetry UI shows success/failure status and observed latency in milliseconds."
    - "INF-05: telemetry UI shows token and cost metadata only when available."
    - "INF-06: unavailable model/provider/token/cost values render as explicit unavailable copy, never numeric zero or raw sentinel internals."
    - "Phase 02 UI must not add Phase 3+ controls for cost/latency/custom routes, fallback simulation, cache/repeat, Langfuse traces, or eval execution."
  artifacts:
    - path: "app.py"
      provides: "Runtime RunHistory wiring into NiceGUI app"
      exports: ["main"]
    - path: "src/openrouter_demo/ui.py"
      provides: "NiceGUI streaming inference console"
      exports: ["build_app"]
    - path: "tests/test_ui.py"
      provides: "UI surface/state regression tests"
      exports: []
  key_links:
    - from: "app.py"
      to: "src/openrouter_demo/history.py"
      via: "Instantiates RunHistory and passes it to build_app"
      pattern: "RunHistory()"
    - from: "src/openrouter_demo/ui.py"
      to: "docs/ux/screen-spec.md"
      via: "Uses required labels and state copy"
      pattern: "Run Inference"
  prohibitions:
    - "No Phase 3 route controls or fallback simulation."
    - "No cache/repeat claims."
    - "No Langfuse trace creation."
    - "No separate frontend or FastAPI product API."
---

<objective>
Wire the Phase 02 NiceGUI surface to the streaming state seam so a reviewer can run default-route inference, watch response text stream, and inspect basic request evidence.

Purpose: This is the user-visible Phase 02 slice that turns the backend and handler into an interview-demonstrable streaming inference console.
Output: `app.py` history wiring, updated `build_app`, streaming response/telemetry/history UI, and focused tests.
</objective>

<scope_note>
This plan depends on 02-01 because the surface should use the tested `_run_inference` seam rather than embedding untestable streaming logic directly in NiceGUI callbacks.
</scope_note>

<execution_context>
@/Users/ishmusic/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ishmusic/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
@.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
@.planning/phases/02-streaming-inference-evidence/02-AI-SPEC.md
@.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
@.planning/phases/02-streaming-inference-evidence/02-VALIDATION.md
@docs/tasks/phase-2-streaming-inference.md
@docs/ux/screen-spec.md
@docs/ux/ui-ux-plan.md
@app.py
@src/openrouter_demo/config.py
@src/openrouter_demo/client.py
@src/openrouter_demo/models.py
@src/openrouter_demo/routing.py
@src/openrouter_demo/history.py
@src/openrouter_demo/ui.py
@tests/test_ui.py
</context>

<artifacts_this_phase_produces>
## Files

- `app.py`
- `src/openrouter_demo/ui.py`
- `tests/test_ui.py`

## Symbols

- `app.main`
- `openrouter_demo.ui.build_app`
- `openrouter_demo.ui._render_telemetry`
- `openrouter_demo.ui._render_history`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire app runtime state and preserve setup guards</name>
  <read_first>
    @.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
    @.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
    @app.py
    @src/openrouter_demo/config.py
    @src/openrouter_demo/history.py
    @src/openrouter_demo/ui.py
    @tests/test_config.py
    @tests/test_ui.py
  </read_first>
  <behavior>
    - `app.main()` creates one `RunHistory()` instance and passes it to `build_app`.
    - `build_app` accepts `config: AppConfig` and `history: RunHistory` while preserving missing-key setup guidance.
    - With missing `OPENROUTER_API_KEY`, `Run Inference` remains disabled and no request handler starts.
    - Langfuse disabled copy remains visible and optional.
  </behavior>
  <files>app.py, src/openrouter_demo/ui.py, tests/test_ui.py</files>
  <action>In `app.py`, import `RunHistory` from `openrouter_demo.history`, instantiate `history = RunHistory()` inside `main()`, and call `build_app(config, history)`. In `src/openrouter_demo/ui.py`, update `build_app(config: AppConfig, history: RunHistory, *, stream_fn=stream_chat_completion) -> None`. Preserve `_status`, `OpenRouter` readiness, `Setup needed`, `Set OPENROUTER_API_KEY in your shell, then restart the app.`, and Langfuse disabled copy. Add or extend `tests/test_ui.py` assertions that `build_app` keeps the missing-key path from attempting a request by leaving the run action disabled or unreachable when `config.openrouter_ready` is false.</action>
  <verify>
    <automated>uv run pytest tests/test_ui.py tests/test_config.py -q</automated>
    <automated>uv run ruff check app.py src/openrouter_demo/ui.py tests/test_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` wires a `RunHistory` instance into `build_app`.
    - Existing Phase 1 missing-credential guidance still appears in `ui.py` source.
    - Missing OpenRouter key cannot trigger `stream_chat_completion` from the UI path.
  </acceptance_criteria>
  <done>Runtime app state is wired without weakening setup and optional Langfuse guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace disabled Phase 1 shell with streaming response, telemetry, and history panels</name>
  <read_first>
    @.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
    @.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
    @docs/ux/screen-spec.md
    @docs/ux/ui-ux-plan.md
    @src/openrouter_demo/ui.py
    @src/openrouter_demo/models.py
    @src/openrouter_demo/routing.py
    @src/openrouter_demo/history.py
    @tests/test_ui.py
  </read_first>
  <behavior>
    - Prompt panel uses placeholder `Ask a production-style question, classification task, or summarization task...`.
    - Primary action text is `Run Inference`.
    - Empty response text is `Run an inference request to see streaming output.`.
    - In-flight status text is `Streaming from OpenRouter...`.
    - Success status text is `Request completed successfully.`.
    - Failure status text is `Request failed before fallback could complete.`.
    - Telemetry panel includes Status, Strategy, Model, Provider, Latency, Tokens, and Cost.
    - Run history renders prior runs with strategy, model, provider, latency, tokens, cost, and status/failure evidence.
  </behavior>
  <files>src/openrouter_demo/ui.py, tests/test_ui.py</files>
  <action>In `src/openrouter_demo/ui.py`, replace the Phase 1 disabled live-inference card with a NiceGUI operations console. Use a prompt textarea, sample prompt controls for the four sample prompts from `docs/ux/screen-spec.md`, a visible default strategy summary (`Default` and `Balanced route for general quality and availability.`), and a `Run Inference` button whose click handler uses the 02-01 `_run_inference` seam. Add response, telemetry, and run-history render helpers, named `_render_telemetry` and `_render_history` or equivalents. Render missing model/provider through `_format_metadata`, missing cost through `_format_cost`, and token absence through explicit unavailable copy. Keep fallback/cache/eval/Langfuse trace controls out of Phase 02; future panels may remain as static out-of-scope notes.</action>
  <verify>
    <automated>uv run pytest tests/test_ui.py -q</automated>
    <automated>uv run pytest</automated>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `src/openrouter_demo/ui.py` contains the required Phase 02 copy strings from `02-UI-SPEC.md`.
    - `Run Inference` is no longer permanently disabled when `OPENROUTER_API_KEY` is configured and prompt text is non-empty.
    - Telemetry and history rendering use explicit unavailable copy for absent metadata.
    - Phase 3+ controls for cost/latency/custom routes, fallback simulation, cache/repeat, Langfuse traces, and eval execution are not implemented.
    - `uv run pytest` and `uv run ruff check .` pass.
  </acceptance_criteria>
  <done>The NiceGUI UI demonstrates streaming inference evidence with status, telemetry, and run history.</done>
</task>

</tasks>

<verification>
Run `uv run pytest`, `uv run ruff check .`, and a manual smoke when `OPENROUTER_API_KEY` is available: `uv run python app.py`, submit a small prompt, observe progressive response text, telemetry update, and a run-history row.
</verification>

<success_criteria>
- User can submit a prompt through the UI and receive a live OpenRouter response.
- Response text appears progressively while the request streams.
- Completed run shows strategy, model/provider when available, latency, and success/failure state.
- Token and cost fields display values only when available and otherwise show unavailable.
</success_criteria>

<threat_model>
| Threat | Severity | Mitigation |
|---|---:|---|
| Missing API key path regresses and sends a request anyway. | high | Preserve setup guard and test disabled/unreachable request path when `openrouter_ready` is false. |
| UI fabricates unavailable telemetry as zero. | high | Use tested formatters and `UNAVAILABLE` sentinel checks. |
| Scope creeps into Phase 3+ controls, making execution larger and less verifiable. | medium | PLAN prohibitions and acceptance criteria explicitly exclude later controls. |
| Duplicate clicks create overlapping streams. | medium | Disable `Run Inference` while a run is active. |
</threat_model>
