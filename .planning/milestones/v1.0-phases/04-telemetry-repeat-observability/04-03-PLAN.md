---
phase: 04-telemetry-repeat-observability
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/openrouter_demo/sqlite_store.py
  - src/openrouter_demo/ui.py
  - src/openrouter_demo/telemetry_schema.py
  - tests/test_sqlite_store.py
  - tests/test_ui.py
  - tests/test_imports.py
autonomous: true
requirements: [OBS-07]

estimate:
  tokens: 65000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A run appended to SQLiteRunHistory and reloaded preserves Unavailable sentinels (is UNAVAILABLE, not {'label': 'unavailable'}) and all cache/trace fields plus fallback_evidence."
    - "Reloaded runs render in the history table with Cache and Trace columns alongside the existing Run/Strategy/Model/Provider/Latency/Tokens/Cost/Fallback columns."
    - "The main UI supports comparison of completed runs (a sortable or side-by-side view of at least model, provider, latency, cost, cache, and trace across recent runs)."
  artifacts:
    - "src/openrouter_demo/sqlite_store.py — _row_to_run rebuilt via TelemetryEvidence.from_dict; fallback_evidence persisted"
    - "src/openrouter_demo/ui.py — _history_rows extended to 10 columns; comparison section in _render_history"
    - "src/openrouter_demo/telemetry_schema.py — removed (dead RunRecord/FallbackAttempt schema; no remaining imports)"
    - "tests/test_sqlite_store.py — round-trip preserves sentinels + cache/trace fields + fallback_evidence"
    - "tests/test_ui.py — 10-column history rows and comparison rendering"
    - "tests/test_imports.py — new Phase 4 types/fields importable"
  key_links:
    - "sqlite_store.append(to_dict) ↔ sqlite_store._row_to_run(from_dict) ↔ ui._history_rows Cache/Trace columns"
  prohibitions:
    - "Must not persist Unavailable as {'label': 'unavailable'} or reload it as a plain dict; the sentinel must round-trip as the sentinel."
    - "Must not drop cache/trace/fallback data on reload."
---

<objective>
Deliver the persistence and comparison slice: completed runs survive save/load with honest `Unavailable` sentinels and the new cache/trace/fallback fields intact, and the main UI shows a comparison-ready run history with Cache and Trace columns.

**As a** forward-deployed engineer, **I want to** compare recent runs (model/provider, latency, tokens, cost, fallback, cache, trace) in one place, **so that** route and cache tradeoffs are visible across runs rather than one run at a time.

Purpose: Close the OBS-07 loop by fixing the `SQLiteRunHistory` round-trip (Pitfall 3) and extending the history UI to a 10-column comparison grid.
Output: Correct `sqlite_store` (de)serialization, 10-column history + comparison section, and the persistence/UI test files.
</objective>

## Artifacts this phase produces

**Extended dataclass fields:**
- `TelemetryEvidence.cache_status`, `.cached_tokens`, `.cache_write_tokens`, `.trace_status`, `.trace_id`, `.trace_url`, `.openrouter_metadata` (`src/openrouter_demo/models.py`)
- `StreamedResult.cache_status`, `.cached_tokens`, `.cache_write_tokens`, `.openrouter_metadata` (`src/openrouter_demo/client.py`)
- `InferenceRun.repeat_observation` (`src/openrouter_demo/models.py`)

**New classes/dataclasses:**
- `RepeatObservation` (`src/openrouter_demo/models.py`, next to `FallbackEvidence` — imported by `scenarios.py`)
- `TraceOutcome` (`src/openrouter_demo/telemetry.py`)

**New functions:**
- `_extract_cache` (`src/openrouter_demo/client.py`)
- `record_trace` (`src/openrouter_demo/telemetry.py`)
- `run_repeat_scenario` (`src/openrouter_demo/scenarios.py`)
- `_run_repeat_inference` (`src/openrouter_demo/ui.py`)
- `TelemetryEvidence.to_dict` / `TelemetryEvidence.from_dict` (`src/openrouter_demo/models.py`)

**New file paths:**
- `tests/test_telemetry.py`
- `tests/test_repeat.py`
- `tests/test_sqlite_store.py`

**Modified files:**
- `src/openrouter_demo/models.py`
- `src/openrouter_demo/client.py`
- `src/openrouter_demo/telemetry.py`
- `src/openrouter_demo/scenarios.py`
- `src/openrouter_demo/sqlite_store.py`
- `src/openrouter_demo/ui.py`
- `tests/test_client.py`
- `tests/test_ui.py`
- `tests/test_phase1_guards.py`
- `tests/test_imports.py`

## Flagged Assumptions (edge probe — unresolved, not silently dropped)

The deterministic edge probe returned 9 unresolved items. The meaningful edges (cache honesty, `Unavailable` sentinels, trace optionality, persistence round-trip) are authored into `must_haves.truths`/`prohibitions` above. The remainder are surfaced here as flagged assumptions for review, not silently dropped:

- **OBS-01 adjacency** ("equal or just-touch elements"): the existing last-seen-wins accumulation means later equal usage values overwrite earlier ones; no merge is required. Assumed — no source spec states it.
- **OBS-01 empty** ("empty/single/null input"): zero-chunk stream → `Status.FAILED` with `telemetry=None`; single-chunk → that chunk's usage; null `usage` → `UNAVAILABLE`. Assumed from existing code paths.
- **OBS-01 ordering** ("equal elements — order stable?"): run history ordering is `started_at DESC` (stable). Assumed — no explicit acceptance criterion exists.
- **OBS-02..OBS-07** ("unclassified — review manually"): no edge taxonomy category applied; resolved by the cache-honesty, sentinel, trace-optionality, and persistence truths authored above.

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-telemetry-repeat-observability/04-RESEARCH.md
@.planning/phases/04-telemetry-repeat-observability/04-PATTERNS.md
@.planning/phases/04-telemetry-repeat-observability/04-VALIDATION.md
@.planning/phases/04-telemetry-repeat-observability/04-02-SUMMARY.md (repeat_observation field on InferenceRun; cache/trace fields produced by Waves 1-2)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistence round-trip — sentinels, cache/trace, and fallback survive save/load</name>
  <files>src/openrouter_demo/sqlite_store.py, tests/test_sqlite_store.py</files>
  <read_first>
    - src/openrouter_demo/sqlite_store.py (append ~lines 43-52, _row_to_run ~lines 82-103, _init_db ~lines 26-40)
    - src/openrouter_demo/models.py (TelemetryEvidence.to_dict/from_dict from Wave 1; FallbackEvidence fields; InferenceRun.fallback_evidence and repeat_observation)
    - .planning/phases/04-telemetry-repeat-observability/04-RESEARCH.md (Pitfall 3 — the round-trip corruption; Pattern for sqlite_store)
  </read_first>
  <action>
    Fix sqlite_store.py round-trip with ONE serialization scheme: a single nested JSON document stored in the existing `telemetry_json` column, shaped `{"telemetry": <TelemetryEvidence.to_dict()> | null, "fallback_evidence": <asdict(FallbackEvidence)> | null, "repeat_observation": <asdict(RepeatObservation)> | null}`. NO new SQL columns and NO ALTER TABLE — the existing column is the whole channel.

    In append(): build the nested document as `payload = {"telemetry": run.telemetry.to_dict() if run.telemetry is not None else None, "fallback_evidence": asdict(run.fallback_evidence) if run.fallback_evidence is not None else None, "repeat_observation": asdict(run.repeat_observation) if run.repeat_observation is not None else None}` and store `json.dumps(payload)`. (This replaces the current flat `json.dumps(asdict(run.telemetry) ...)`.)

    In _row_to_run(): parse `doc = json.loads(row["telemetry_json"])`. Branch on shape — if the document is the NEW nested form (a dict containing a `"telemetry"` key), rebuild telemetry via `TelemetryEvidence.from_dict(doc["telemetry"])` (from_dict consumes exactly the `dict["telemetry"]` value, i.e. the TelemetryEvidence-shaped dict, NOT the outer wrapper — do not pass the whole wrapper and do not coerce a missing `latency_ms` to 0; use the stored int), rebuild FallbackEvidence field-by-field from `doc["fallback_evidence"]` (AttemptRecord + AttemptRecord + simulated bool) when not None, and rebuild RepeatObservation field-by-field from `doc["repeat_observation"]` (first/second StreamedResult + cache fields) when not None; pass `fallback_evidence` and `repeat_observation` into InferenceRun so comparison and Cache-delta copy survive restart. If the document is the LEGACY flat form (a dict WITHOUT a `"telemetry"` key — pre-existing `data/runs.db` rows written before this wave), apply a compatibility branch that reconstructs the old 7-field TelemetryEvidence (and sets fallback_evidence=None, repeat_observation=None) so old rows still load instead of raising; note that old flat rows will NOT round-trip back to the new nested form until they are re-appended. Keep `from_dict` tolerant: it must accept only the TelemetryEvidence-shaped dict, and the append/_row_to_run pair must never mix the flat and nested shapes.

    Create tests/test_sqlite_store.py with a pytest tmp_path fixture: construct an InferenceRun with TelemetryEvidence(model=UNAVAILABLE, provider="OpenAI", latency_ms=12, prompt_tokens=UNAVAILABLE, completion_tokens=UNAVAILABLE, total_tokens=UNAVAILABLE, cost_usd=UNAVAILABLE, cache_status="hit", cached_tokens=10, cache_write_tokens=0, trace_status="disabled", trace_id=None, trace_url=None) and a FallbackEvidence; append then reload via store.get(run_id); assert reloaded.telemetry.model is UNAVAILABLE (identity, not a dict), reloaded.telemetry.cached_tokens == 10, reloaded.telemetry.trace_status == "disabled", reloaded.telemetry.cost_usd is UNAVAILABLE, and reloaded.fallback_evidence is not None. Add a second test with cache_status is UNAVAILABLE and assert it reloads as UNAVAILABLE (not {"label": "unavailable"}). Add a third test that writes a legacy flat `{"model": ..., "provider": ..., "latency_ms": 12, ...}` document directly into a row and asserts `_row_to_run`/`store.get` still loads it without raising (compatibility branch) with fallback_evidence None.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sqlite_store.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Reloaded `TelemetryEvidence.model is UNAVAILABLE` and `telemetry.cost_usd is UNAVAILABLE` (sentinel identity preserved).
    - Reloaded `telemetry.cached_tokens == 10` and `telemetry.trace_status == "disabled"`.
    - Reloaded `fallback_evidence is not None` and `repeat_observation` survives when present.
    - `TelemetryEvidence.from_dict` consumes exactly `doc["telemetry"]` (the TelemetryEvidence-shaped dict); `append()` stores one nested document `{"telemetry": ..., "fallback_evidence": ..., "repeat_observation": ...}`.
    - A legacy flat-format row still loads via the compatibility branch (does not raise) and reports `fallback_evidence is None`.
    - No `{"label": "unavailable"}` string appears in the reloaded telemetry attributes.
  </acceptance_criteria>
  <done>SQLiteRunHistory round-trips Unavailable sentinels, cache/trace fields, and fallback/repeat evidence without corruption; `uv run pytest tests/test_sqlite_store.py -q` is green.</done>
</task>

<task type="auto">
  <name>Task 2: History comparison — Cache/Trace columns and a comparison view</name>
  <files>src/openrouter_demo/ui.py, tests/test_ui.py, tests/test_imports.py</files>
  <read_first>
    - src/openrouter_demo/ui.py (_history_rows ~line 153, _render_history ~line 183, _format_* helpers, _telemetry_rows Router/Cache/Trace rows from Wave 1)
    - src/openrouter_demo/models.py (TelemetryEvidence cache/trace/openrouter_metadata fields, RepeatObservation, TraceOutcome in telemetry.py)
    - tests/test_ui.py — three _history_rows shape-dependent assertions to update: test_history_rows_include_fallback_column ~line 239 (len(rows[0]) == 8), test_telemetry_and_history_rows_render_unavailable_copy ~line 155 (`[0][2:] == (6-tuple)`), test_history_rows_include_fallback_column ~line 274 (`rows[0][-1] == "—"`), and ~line 321 (`rows[1][-1] == "Yes"`)
    - tests/test_imports.py (extend the importable-types test)
    - .planning/phases/04-telemetry-repeat-observability/04-PATTERNS.md (ui.py "History rows pattern" section)
  </read_first>
  <action>
    Extend ui.py _history_rows from 8 to 10 columns: append a cache label and a trace label to each row. cache label = "hit"/"write" with token count when telemetry.cache_status is "hit"/"write", else "—" (or "Observed repeat" when repeat_observation is present with UNAVAILABLE cache); trace label = trace_url or trace_id when trace_status == "enabled", "disabled" when trace_status == "disabled", "failed" when "failed", else "—". Update _render_history columns tuple to ("Run", "Strategy", "Model", "Provider", "Latency", "Tokens", "Cost", "Fallback", "Cache", "Trace"). Add a comparison section: below the history grid, render a compact comparison table of the most recent completed runs (limit 10) with at least Model, Provider, Latency, Cost, Cache, and Trace columns, reusing the _format_* helpers (this satisfies OBS-07 "comparison of completed runs in the main UI"). Keep the empty-history placeholder copy. Remove the now-obsolete "Future operation panels" card (the card whose label is "Cache, trace links, and eval execution stay reserved for later phases") since Cache and Trace are now real; the `LANGFUSE_ENV_VARS` copy is superseded by real trace rendering.

    Update tests/test_ui.py — enumerate ALL THREE existing _history_rows shape-dependent assertions that break when history columns go 8→10, not just the len() check:
    (a) test_telemetry_and_history_rows_render_unavailable_copy (~line 155) asserts `_history_rows(history)[0][2:] == (6-tuple)`. That tuple becomes an 8-tuple once Cache and Trace are appended: keep the 6 existing values unchanged and append the two new cells — for this run (no cache, trace_status defaulted) append `("—", "—")` so `[0][2:]` equals (model, provider, "15 ms", tokens, cost, "—", "—", "—").
    (b) test_history_rows_include_fallback_column (~line 274) asserts `rows[0][-1] == "—"`; after the change this indexes the Trace cell and only passes coincidentally. Replace it with explicit `rows[0][-3] == "—"` (Fallback, absent for this run), `rows[0][-2] == "—"` (Cache), and `rows[0][-1] == "—"` (Trace) — so the absent-fallback cell keeps explicit coverage rather than being silently dropped.
    (c) test_history_rows_include_fallback_column (~line 321) asserts `rows[1][-1] == "Yes"`; after the change this indexes the Trace cell and will fail because Fallback "Yes" moves from index -1 to -3. Change it to `rows[1][-3] == "Yes"` and add `rows[1][-2] == "—"` (Cache) and `rows[1][-1] == "—"` (Trace).
    Also change the `len(rows[0]) == 8` assertion in test_history_rows_include_fallback_column to `== 10`. Add a test that a run with cache_status="hit" and trace_status="enabled" renders "hit" and the trace_url/id in the last two columns. Add a test asserting the comparison section renders ≥1 row for ≥1 completed run.

    Extend tests/test_imports.py: add assertions that RepeatObservation, TraceOutcome, and run_repeat_scenario are importable, and that TelemetryEvidence has the seven new fields (use dataclasses.fields(TelemetryEvidence) to assert "cache_status", "trace_status", and "openrouter_metadata" are present).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ui.py tests/test_imports.py tests/test_sqlite_store.py -q && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `_history_rows(history)[0]` has length 10; the last two cells are the Cache and Trace labels.
    - The three existing shape-dependent assertions in tests/test_ui.py are updated: (a) `_history_rows(history)[0][2:]` equals an 8-tuple with the two new Cache+Trace cells appended; (b) `rows[0][-3] == "—"` (Fallback absent), `rows[0][-2] == "—"` (Cache), and `rows[0][-1] == "—"` (Trace); (c) `rows[1][-3] == "Yes"`, `rows[1][-2] == "—"`, `rows[1][-1] == "—"`.
    - `_render_history` columns tuple is the 10-column tuple ending in "Cache", "Trace".
    - `dict(_telemetry_rows(run))` still contains "Cache" and "Trace" (no regression from Wave 1).
    - `tests/test_imports.py` passes with `RepeatObservation`, `TraceOutcome`, `run_repeat_scenario` importable and `TelemetryEvidence` exposing the seven new fields (`cache_status`, `trace_status`, `openrouter_metadata` present via `dataclasses.fields`).
    - `uv run ruff check .` exits 0.
  </acceptance_criteria>
  <done>History shows 10 columns including Cache and Trace, a comparison view of recent runs exists, and the full suite plus lint is green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead telemetry_schema.py — one telemetry schema of truth</name>
  <files>src/openrouter_demo/telemetry_schema.py</files>
  <read_first>
    - src/openrouter_demo/telemetry_schema.py (RunRecord + FallbackAttempt — the dead competing schema)
    - grep -rn "telemetry_schema" src/ tests/ (confirm zero imports — verified: only .planning/ docs mention it)
    - .planning/phases/04-telemetry-repeat-observability/04-RESEARCH.md (Anti-Patterns to Avoid — "Leaving two competing telemetry schemas")
  </read_first>
  <action>
    Delete src/openrouter_demo/telemetry_schema.py and remove any references. Verified in read_first that no file in src/ or tests/ imports `telemetry_schema` and no code references `RunRecord`/`FallbackAttempt` (the live schemas are models.TelemetryEvidence and models.FallbackEvidence). After deleting, confirm no dangling import remains: `grep -rn "telemetry_schema" src/ tests/` returns nothing and `grep -rn "RunRecord\|FallbackAttempt" src/ tests/` returns nothing. Do not delete anything else and do not touch models.py — this task only removes the orphaned file.
  </action>
  <verify>
    <automated>uv run pytest -q && ! grep -rn "telemetry_schema" src/ tests/ && ! grep -rn "RunRecord\|FallbackAttempt" src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `src/openrouter_demo/telemetry_schema.py` no longer exists.
    - No import of `telemetry_schema` remains in `src/` or `tests/` (grep returns nothing).
    - No reference to `RunRecord`/`FallbackAttempt` remains in `src/` or `tests/`.
    - `uv run pytest -q` stays green after the deletion.
  </acceptance_criteria>
  <done>The repo has one telemetry schema (models.TelemetryEvidence); `uv run pytest -q` is green and no `telemetry_schema` import remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sqlite_store ↔ data/runs.db | Local file; our own serialized run payloads are the only writer. |
| ui.py → NiceGUI browser | Untrusted model text rendered into history/comparison cells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | `sqlite_store.py` SQL | low | accept | Parameterized SQL (`?` placeholders) already in place; the single-document JSON approach adds no new SQL surface. Canon — covered by `/gsd:secure-phase`. |
| T-04-10 | Information Disclosure | `sqlite_store.py` serialization | low | mitigate | Serialization never stores the OpenRouter API key or Langfuse secret key (only readiness-derived trace_status/id/url and telemetry); secrets live in env vars only. |
| T-04-11 | Spoofing | `ui.py` comparison cells | low | mitigate | Comparison cells reuse `_format_*` helpers; `Unavailable` renders as unavailable copy, never as a fabricated zero/value. |
| T-04-SC | Tampering | package installs | n/a | accept | No new packages this phase. |

*Blocking threshold is "high"; no threat reaches high severity, so `autonomous: true` holds.*
</threat_model>

<verification>
- `uv run pytest tests/test_sqlite_store.py -q` (Task 1)
- `uv run pytest tests/test_ui.py tests/test_imports.py tests/test_sqlite_store.py -q && uv run ruff check .` (Task 2)
- `uv run pytest -q && ! grep -rn "telemetry_schema" src/ tests/ && ! grep -rn "RunRecord\|FallbackAttempt" src/ tests/` (Task 3)
- Phase gate: `uv run pytest && uv run ruff check .` both green before `/gsd-verify-work`.
</verification>

<success_criteria>
- Persisted runs reload with sentinels and cache/trace/fallback/repeat fields intact.
- The history table has Cache and Trace columns; a comparison view of recent runs is visible in the main UI.
- The obsolete "Future operation panels" card is gone.
- `src/openrouter_demo/telemetry_schema.py` is removed; the repo has one telemetry schema (`models.TelemetryEvidence`).
</success_criteria>

<output>
Create `.planning/phases/04-telemetry-repeat-observability/04-03-SUMMARY.md` when done.
</output>
