---
phase: 06-interview-walkthrough-and-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/architecture.md
  - docs/failure-tree.md
  - docs/specs/quickstart.md
  - docs/specs/failure-tree.md
  - tests/test_docs.py
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

estimate:
  tokens: 40000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reviewer following README.md can install (uv sync), configure the four env vars, launch (uv run python app.py), and run the five-minute walkthrough without encountering stale Phase-1 'not implemented' claims."
    - "docs/architecture.md exists and describes routing, fallback, latency, cost, telemetry, and eval flow via a ## Component Boundaries table and a mermaid flowchart LR data-flow diagram."
    - "docs/failure-tree.md exists and covers all eight failure categories (client, credential, request, provider, routing, timeout, telemetry, display), with its User-facing copy blocks reconciled to the literal ui.py constants."
    - "docs/specs/quickstart.md step 6 shows the canonical command PYTHONPATH=src uv run python -m openrouter_demo.evals, and its 'Files expected after implementation' block lists README.md, docs/failure-tree.md, docs/architecture.md."
    - "tests/test_docs.py pins the docs to the code (architecture existence, README canonical eval command, failure-tree/quickstart path resolution) and passes."
  artifacts:
    - "README.md — rewritten (story, setup, env vars, walkthrough; six pinned strings preserved)"
    - "docs/architecture.md — created by promoting .planning/research/ARCHITECTURE.md"
    - "docs/failure-tree.md — created by moving docs/specs/failure-tree.md"
    - "docs/specs/failure-tree.md — deleted after the move"
    - "docs/specs/quickstart.md — eval command + file links fixed"
    - "tests/test_docs.py — created (doc-vs-code drift guard)"
  key_links:
    - "docs/architecture.md -> .planning/research/ARCHITECTURE.md ## Component Boundaries + mermaid diagram"
    - "README.md -> docs/specs/quickstart.md, docs/failure-tree.md, docs/architecture.md"
    - "tests/test_docs.py -> Path(...).read_text() guards for doc-vs-code drift"
  prohibitions:
    - "Must not document real API keys in any doc; keep .env.example as empty assignments only."
    - "Must not add new dependencies (no MkDocs/Sphinx/markdownlint); Markdown in docs/ is the convention."
    - "Must not modify src/ code (Phases 1-5 are complete and green)."
    - "Must not treat NiceGUI's internal FastAPI as a product layer in docs/architecture.md."
---

<objective>
Make the repository's three reviewer-facing documents truthful and complete: rewrite the stale `README.md` (DOC-01), create the missing `docs/architecture.md` (DOC-02), and move/reconcile the failure tree to the `docs/failure-tree.md` path that `quickstart.md` already expects (DOC-03) — all pinned against future drift by a new `tests/test_docs.py` guard.

**As a** forward-deployed engineer reviewing this repo, **I want to** follow the project story from README, understand the architecture from a dedicated guide, and debug failures from a failure tree whose paths and UI copy match the actual code, **so that** I can trust the docs instead of re-reading the source.

Purpose: Prove the doc-vs-code drift-guard mechanism end-to-end on the thinnest slice (one doc + one pinning test) before expanding to the README rewrite and the failure-tree move.
Output: A rewritten `README.md`, a new `docs/architecture.md`, a relocated `docs/failure-tree.md`, a fixed `docs/specs/quickstart.md`, and a new `tests/test_docs.py` drift guard.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `README.md` | modified (full rewrite) | Plan 06-01 Task 2 |
| `docs/architecture.md` | created (promoted from `.planning/research/ARCHITECTURE.md`) | Plan 06-01 Task 1 |
| `docs/failure-tree.md` | created (moved from `docs/specs/failure-tree.md`) | Plan 06-01 Task 3 |
| `docs/specs/failure-tree.md` | deleted (source of the move) | Plan 06-01 Task 3 |
| `docs/specs/quickstart.md` | modified (eval command + file links fixed) | Plan 06-01 Task 3 |
| `tests/test_docs.py` | created then extended (drift guard) | Plan 06-01 Tasks 1/2/3; Plan 06-02 Task 2 |
| `tests/test_ui.py` | modified (DOC-04 framing guard) | Plan 06-02 Task 1 |

## Flagged Assumptions (no SPEC/CONTEXT for this phase)

- **DOC-01 README structure:** no SPEC prescribes README headings; use `#`/`##` markdown with the six pinned substrings preserved verbatim. Assumed — driven by `tests/test_config.py::test_readme_documents_setup`.
- **DOC-02 architecture guide headings:** promote the `.planning/research/ARCHITECTURE.md` section names (`## Component Boundaries`, `## Data Flow`, `## Patterns to Follow`, `## Anti-Patterns to Avoid`) rather than inventing a new structure. Assumed — RESEARCH "Architectural Responsibility Map" marks this as a promote-not-invent task.
- **DOC-03 failure-tree move:** use a move (source file deleted) rather than a copy, so exactly one canonical failure-tree path exists. Assumed — `quickstart.md` expects only `docs/failure-tree.md`.

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-RESEARCH.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-PATTERNS.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@tests/test_config.py
@tests/test_imports.py
@tests/test_phase1_guards.py
@src/openrouter_demo/ui.py
@src/openrouter_demo/config.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — create docs/architecture.md and pin it with tests/test_docs.py</name>
  <precondition>Phases 1-5 code is complete and green — verify with `uv run pytest -q` (expect 100 passed) before touching docs.</precondition>
  <files>docs/architecture.md, tests/test_docs.py</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (source of truth: ## Component Boundaries 9-row table, the mermaid flowchart LR diagram, ## Data Flow 7 steps, ## Patterns to Follow, ## Anti-Patterns to Avoid)
    - tests/test_config.py (guard-test style: Path(...).read_text() + plain assert, lines 57-79)
    - tests/test_imports.py (file-existence + Path(...).read_text() pattern, lines 88-94)
    - src/openrouter_demo/ui.py (entry-point copy constants at lines 116-122, for accurate references)
    - src/openrouter_demo/config.py (REQUIRED_ENV_VARS / LANGFUSE_ENV_VARS at lines 10-11)
  </read_first>
  <action>
    Create docs/architecture.md by promoting the architecture content from .planning/research/ARCHITECTURE.md (do not invent new architecture; this phase does not change src/ code). Use these exact section headings in order: `# Architecture`, `## Component Boundaries`, `## Data Flow`, `## Patterns to Follow`, `## Anti-Patterns to Avoid`. Copy the Component Boundaries table verbatim (9 rows: app.py, config.py, routing.py, client.py, models.py, telemetry.py, scenarios.py, evals.py, ui.py, each with its Responsibility and Communicates With columns). Copy the mermaid `flowchart LR` diagram verbatim (nodes A..J: Reviewer browser -> ui.py -> scenarios.py -> routing.py/client.py -> OpenRouter API, StreamedResult -> models.py, record_trace -> telemetry.py -> optional Langfuse, TelemetryEvidence -> ui.py, EvalCase scoring -> evals.py -> client.py). Copy the ## Data Flow 7 numbered steps, the four ## Patterns to Follow subsections (Async UI Without Blocking, Router Metadata Opt-In, Honest Missing Data, Optional Observability), and the four ## Anti-Patterns to Avoid subsections (Hiding OpenRouter Behind Another Router, Silent Fallback, Blocking the NiceGUI Event Loop, Treating FastAPI as a Product Layer).

    Create tests/test_docs.py in the exact style of tests/test_config.py (plain `def test_...() -> None` with plain `assert`, `from pathlib import Path`, repo-root-relative paths). Define one test function `test_architecture_guide_exists() -> None` that asserts `Path("docs/architecture.md").exists()` is True and that the exact heading string `## Component Boundaries` is in `Path("docs/architecture.md").read_text()`. Do not add any other test functions yet — later tasks in this plan extend the same file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/architecture.md` exits 0.
    - `grep -c "## Component Boundaries" docs/architecture.md` returns >= 1 (the `grep -v '^#'` filter is not needed here; this is a positive presence check, not a bare `== 0` gate).
    - `grep -c "flowchart LR" docs/architecture.md` returns >= 1.
    - `grep -c "## Anti-Patterns to Avoid" docs/architecture.md` returns >= 1.
    - `uv run pytest tests/test_docs.py -q` reports 1 passed, 0 failed.
  </acceptance_criteria>
  <reversibility rating="reversible">New docs and test file; revert via git.</reversibility>
  <done>docs/architecture.md exists with the Component Boundaries table and mermaid data-flow diagram; tests/test_docs.py pins its existence and the drift-guard mechanism is proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for DOC-01 — story, setup, env vars, five-minute walkthrough</name>
  <files>README.md, tests/test_docs.py</files>
  <read_first>
    - README.md (current stale file — the headings and claim paragraphs to replace)
    - tests/test_config.py (test_readme_documents_setup at lines 65-75 — the six pinned substrings)
    - docs/ux/demo-script.md (30-second pitch + 2-minute/5-minute walkthrough segments)
    - docs/ux/demo-narrative.md (story structure; the UI-is-an-operating-surface intent)
    - docs/specs/quickstart.md (8 validation steps; step 6 canonical eval command)
    - src/openrouter_demo/config.py (REQUIRED_ENV_VARS = (OPENROUTER_API_KEY,); LANGFUSE_ENV_VARS)
    - .env.example (the four empty-assignment vars)
    - tests/test_docs.py (created in Task 1; append a new test function here)
  </read_first>
  <action>
    Rewrite README.md so it explains the demo story, setup, env vars, and the five-minute walkthrough (DOC-01). Delete the stale status section and its implemented/not-implemented claim paragraphs entirely (they appear as the second top-level section in the current README.md; see <read_first> for the exact stale strings to remove — do not reproduce those stale claims in the new file). Fix the sentence that claims the app does not parse .env files only in Phase 1: state instead that the app reads exported environment variables only and never parses .env files (config.py reads os.environ directly; there is no python-dotenv dependency).

    Structure the rewritten README.md with these sections in order: `# OpenRouter Production Inference Lab`, `## What this demo shows` (the story: route, observe, recover, and evaluate model calls; inference operation, not a chatbot), `## Prerequisites` (Python 3.12+, uv), `## Install` (the `uv sync` command), `## Configure` (the four env vars as empty export assignments; OPENROUTER_API_KEY is required, LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY / LANGFUSE_BASE_URL are optional and disable tracing when absent), `## Launch` (the `uv run python app.py` command), `## Five-minute walkthrough` (link to docs/ux/demo-script.md and docs/specs/quickstart.md), `## Run the evals` (the canonical command PYTHONPATH=src uv run python -m openrouter_demo.evals), `## Quality gates` (uv run pytest and uv run ruff check .), `## Docs` (link docs/architecture.md and docs/failure-tree.md).

    Preserve these six substrings verbatim somewhere in the file (they are pinned by tests/test_config.py::test_readme_documents_setup): `uv sync`, `uv run python app.py`, `OPENROUTER_API_KEY`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_BASE_URL`.

    Append a test function to tests/test_docs.py: `def test_readme_documents_eval_command() -> None` that asserts the exact substring `PYTHONPATH=src uv run python -m openrouter_demo.evals` is in `Path("README.md").read_text()`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py tests/test_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py::test_readme_documents_setup -q` passes (six pinned substrings survive).
    - `grep -c "PYTHONPATH=src uv run python -m openrouter_demo.evals" README.md` returns >= 1.
    - `grep -c "Five-minute walkthrough" README.md` returns >= 1.
    - `grep -c "What this demo shows" README.md` returns >= 1.
    - README.md no longer contains the stale status heading `## Phase 1 status` and no longer contains the claim phrase `Not implemented yet`.
    - `uv run pytest tests/test_config.py tests/test_docs.py -q` reports all passed, 0 failed.
  </acceptance_criteria>
  <reversibility rating="reversible">Docs rewrite + one test addition; revert via git.</reversibility>
  <done>README.md rewritten per DOC-01 with the six pinned strings preserved and the canonical eval command documented; tests/test_docs.py now guards the README eval command and the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Move failure tree to docs/failure-tree.md and fix quickstart (DOC-03)</name>
  <files>docs/failure-tree.md, docs/specs/failure-tree.md, docs/specs/quickstart.md, tests/test_docs.py</files>
  <read_first>
    - docs/specs/failure-tree.md (the full tree to move — top-level headings, ## High-level tree, ## Diagnosis path 7 steps, ## Failure examples, ## Debugging rule of thumb)
    - docs/specs/quickstart.md (step 6 eval command at line 80; 'Files expected after implementation' block at lines 94-95)
    - src/openrouter_demo/ui.py (literal user-facing copy constants at lines 116-122: EMPTY_RESPONSE, STREAMING_RESPONSE, SUCCESS_RESPONSE, FAILURE_RESPONSE, FALLBACK_SUCCESS_RESPONSE, SIMULATED_FAILURE_LABEL, TRACE_DISABLED, plus the _format_metadata/_format_cost unavailable strings)
    - .planning/STATE.md (canonical eval CLI decision)
    - tests/test_docs.py (created in Task 1, extended in Task 2; append one more test function here)
  </read_first>
  <action>
    Move docs/specs/failure-tree.md to docs/failure-tree.md (delete the source path after the move so exactly one canonical failure-tree exists). Preserve the top-level structure verbatim: `# Failure Tree - OpenRouter Production Inference Lab`, `## Purpose`, `## High-level tree`, `## Diagnosis path`, `## Failure examples`, `## Debugging rule of thumb`, and the six top-level branches in `## High-level tree` (Client / Python, Authentication / API, OpenRouter routing, Runtime, Observability, Application UI) covering the eight DOC-03 categories (client, credential, request, provider, routing, timeout, telemetry, display).

    Reconcile the illustrative User-facing copy snippets with the literal ui.py constants. Read src/openrouter_demo/ui.py and replace each snippet with the matching literal constant string, or annotate the block with `(illustrative example — not a literal UI string)` where no constant matches. Apply these mappings: the auth/rate-limit snippets map to FAILURE_RESPONSE (literal `Request failed before fallback could complete.`); the routing-unavailable snippet maps to FAILURE_RESPONSE; the runtime/timeout snippet maps to FALLBACK_SUCCESS_RESPONSE (literal `Completed via fallback route after primary route failed.`); the fallback Good/Bad state snippets map to FALLBACK_SUCCESS_RESPONSE and SUCCESS_RESPONSE (literal `Request completed successfully.`) respectively; the telemetry snippet `Langfuse tracing disabled. Configure Langfuse credentials to enable trace links.` already matches TRACE_DISABLED so keep it; the cost snippet maps to the literal `Cost metadata was not returned for this route/provider.` returned by _format_cost for the UNAVAILABLE sentinel. Any remaining snippet with no literal match (e.g., the cache-repeat example) must be annotated as illustrative, never presented as an actual UI string.

    Fix docs/specs/quickstart.md with two surgical edits: (1) in step 6, replace the eval command so the step shows the canonical form `PYTHONPATH=src uv run python -m openrouter_demo.evals` (the old form omits PYTHONPATH=src and raises ModuleNotFoundError because [tool.uv] package = false uses a src layout); (2) confirm the 'Files expected after implementation' block lists exactly `README.md`, `docs/failure-tree.md`, `docs/architecture.md`.

    Append a test function to tests/test_docs.py: `def test_failure_tree_and_quickstart_paths_resolve() -> None` that asserts `Path("docs/failure-tree.md").exists()`, asserts `Path("docs/specs/failure-tree.md").exists()` is False, asserts the substring `## High-level tree` is in `Path("docs/failure-tree.md").read_text()`, and asserts the substring `PYTHONPATH=src uv run python -m openrouter_demo.evals` is in `Path("docs/specs/quickstart.md").read_text()`. In the same test function, read `Path("docs/failure-tree.md").read_text()` into a variable and assert each of the eight DOC-03 categories survives the move by asserting a category-representative concrete substring is present: client -> `malformed request`, credential -> `invalid API key`, request -> `request validation error`, provider -> `provider unavailable`, routing -> `routing constraint too narrow`, timeout -> `timeout`, telemetry -> `token metadata missing`, display -> `fallback hidden from user`. These are positive presence checks of literals the moved tree already contains; do not collapse them into a single branch-label check.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/failure-tree.md` exits 0 and `test -f docs/specs/failure-tree.md` exits 1 (moved, not copied).
    - `grep -c "## High-level tree" docs/failure-tree.md` returns >= 1, and `grep -c "## Diagnosis path" docs/failure-tree.md` returns >= 1.
    - Each of the eight DOC-03 categories is represented in docs/failure-tree.md: `grep -c` for `malformed request` (client), `invalid API key` (credential), `request validation error` (request), `provider unavailable` (provider), `routing constraint too narrow` (routing), `timeout` (timeout), `token metadata missing` (telemetry), and `fallback hidden from user` (display) each returns >= 1.
    - `grep -c "Request failed before fallback could complete." docs/failure-tree.md` returns >= 1 (literal FAILURE_RESPONSE present).
    - `grep -c "Langfuse tracing disabled. Configure Langfuse credentials to enable trace links." docs/failure-tree.md` returns >= 1 (literal TRACE_DISABLED present).
    - `grep -c "PYTHONPATH=src uv run python -m openrouter_demo.evals" docs/specs/quickstart.md` returns >= 1.
    - `uv run pytest tests/test_docs.py -q` reports all passed, 0 failed.
  </acceptance_criteria>
  <reversibility rating="reversible">File move + two doc edits + one test addition; revert via git.</reversibility>
  <done>Failure tree lives at docs/failure-tree.md with its eight categories intact and User-facing copy reconciled to literal ui.py constants; quickstart step 6 and its file links are corrected; tests/test_docs.py pins the path resolution and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README.md -> git history | Human-written docs must never contain real API keys |
| docs -> reviewer | Reviewer-facing docs must not overstate implemented behavior |
| client.py -> Langfuse trace input | Trace input must never contain the API key |
| client.py -> OpenRouter | Live inference spend must stay bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure | README.md / docs/* / git | high | mitigate | `.env.example` keeps empty assignments only; never document real keys; guard tests assert trace input contains no API key |
| T-06-02 | DoS (self-inflicted) | eval prompts / live inference | medium | mitigate | small bounded default prompts + 5 checked-in eval cases + `request_timeout=60.0` |
| T-06-03 | Information disclosure | telemetry.py trace input | high | mitigate | existing `tests/test_evals.py` / `tests/test_telemetry.py` assert no API key in trace input |
| T-06-04 | Availability | telemetry.py `record_trace` | medium | mitigate | `record_trace` catches all exceptions, returns `failed`, never raises |

This phase installs zero new packages, so no package-legitimacy (T-06-SC) threat applies.
</threat_model>

<verification>
- `uv run pytest tests/test_docs.py -q` green.
- `uv run pytest tests/test_config.py tests/test_docs.py -q` green.
- `git status` shows README.md, docs/architecture.md, docs/failure-tree.md, docs/specs/quickstart.md, tests/test_docs.py changed and docs/specs/failure-tree.md deleted.
</verification>

<success_criteria>
- README explains the story, setup, env vars, and five-minute walkthrough with all six pinned strings intact.
- docs/architecture.md exists with Component Boundaries + mermaid + patterns/anti-patterns.
- docs/failure-tree.md exists at the quickstart-expected path with all eight categories and reconciled UI copy.
- docs/specs/quickstart.md shows the canonical eval command and correct file links.
- tests/test_docs.py pins all three docs and passes.
</success_criteria>

<output>
Create `.planning/phases/06-interview-walkthrough-and-quality-gates/06-01-SUMMARY.md` when done
</output>
