---
phase: 06-interview-walkthrough-and-quality-gates
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/test_ui.py
  - tests/test_docs.py
autonomous: true
requirements: [DOC-04, DOC-05]

estimate:
  tokens: 22000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tests/test_ui.py pins DOC-04: the UI source text contains the inference-operation copy and contains none of the forbidden conversation-assistant vocabulary, with no change to src/openrouter_demo/ui.py."
    - "tests/test_docs.py pins DOC-05: each of the four focus areas (response/error, routing configuration, telemetry normalization, eval scoring) maps to at least one existing, passing test file and named test function."
    - "All four focus areas are confirmed covered by existing tests; no new production tests are added unless a focus area is genuinely uncovered."
  artifacts:
    - "tests/test_ui.py — new test_ui_has_no_chatbot_labels() guard"
    - "tests/test_docs.py — new test_focused_test_coverage() guard"
  key_links:
    - "test_ui_has_no_chatbot_labels -> Path('src/openrouter_demo/ui.py').read_text() (source inspection, no execution)"
    - "test_focused_test_coverage -> tests/test_client.py, tests/test_routing.py, tests/test_telemetry.py, tests/test_evals.py, tests/test_sqlite_store.py"
  prohibitions:
    - "Must not modify src/openrouter_demo/ui.py (DOC-04 is already satisfied by code)."
    - "Must not add an LLM judge, tone score, or any new eval mechanism (deferred to V2-01)."
    - "Must not assert the bare eval command as absent (it is a substring of the canonical PYTHONPATH=src form)."
---

<objective>
Add two guard tests that pin already-correct code against future drift: a DOC-04 guard asserting the UI keeps inference-operation framing with no conversation-assistant labels, and a DOC-05 guard asserting the four required focus areas remain covered by existing tests.

**As a** maintainer of this demo, **I want to** lock the UI's inference-operation metaphor and the four focused test-coverage areas behind automated guards, **so that** a future refactor cannot silently reintroduce chatbot framing or delete a required test area.

Purpose: Convert DOC-04 and DOC-05 from "verified by research" into "enforced by tests" so the quality-gate confirmation in Plan 06-03 has teeth.
Output: `tests/test_ui.py` gains one framing guard; `tests/test_docs.py` gains one coverage guard. No `src/` code changes.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `tests/test_ui.py` | modified (DOC-04 framing guard added) | Plan 06-02 Task 1 |
| `tests/test_docs.py` | extended (DOC-05 coverage guard added) | Plan 06-02 Task 2 |
| `README.md` | modified (rewrite) | Plan 06-01 Task 2 |
| `docs/architecture.md` | created | Plan 06-01 Task 1 |
| `docs/failure-tree.md` | created (moved) | Plan 06-01 Task 3 |
| `docs/specs/failure-tree.md` | deleted (moved) | Plan 06-01 Task 3 |
| `docs/specs/quickstart.md` | modified (eval command + links) | Plan 06-01 Task 3 |
| `tests/test_docs.py` | created (drift guard) | Plan 06-01 Tasks 1/2/3 |

## Flagged Assumptions (no SPEC/CONTEXT for this phase)

- **DOC-04 forbidden vocabulary list:** the guard asserts absence of quoted-role strings and UI-label terms; the exact list is chosen so every term is verifiably absent from `ui.py` (run the test to confirm zero false positives). Assumed — RESEARCH "Current State Audit" DOC-04 verified no chatbot labels; the guard only pins that observation.
- **DOC-05 coverage mapping:** the four focus areas map to the test files RESEARCH already enumerated; the guard asserts file existence plus at least one named test function per area rather than asserting an exact test count (which changes as tests grow). Assumed — REQUIREMENTS DOC-05 names four areas, not exact test names.

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-RESEARCH.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-PATTERNS.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-VALIDATION.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-01-SUMMARY.md
@tests/test_ui.py
@tests/test_phase1_guards.py
@tests/test_docs.py
@tests/test_client.py
@tests/test_routing.py
@tests/test_telemetry.py
@tests/test_evals.py
@tests/test_sqlite_store.py
@src/openrouter_demo/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DOC-04 UI-framing guard to tests/test_ui.py</name>
  <files>tests/test_ui.py</files>
  <read_first>
    - tests/test_ui.py (existing tests — append, do not modify existing functions)
    - tests/test_phase1_guards.py (source-inspection pattern: Path(...).read_text() + assert-absence, lines 1-15)
    - src/openrouter_demo/ui.py (the literal inference copy and panel labels to assert present)
    - .planning/phases/06-interview-walkthrough-and-quality-gates/06-PATTERNS.md (DOC-04 guard example and forbidden-vocabulary list)
  </read_first>
  <action>
    Add one test function to tests/test_ui.py: `def test_ui_has_no_chatbot_labels() -> None`. Import `Path` from `pathlib` at the top of the file if it is not already imported. The test reads `text = Path("src/openrouter_demo/ui.py").read_text()` and asserts two things: (1) the inference-operation copy is present — assert each of these exact substrings is in `text`: `ui.page_title("OpenRouter Production Inference Lab")`, `ui.label("Route, observe, recover, and evaluate model calls.")`, `ui.label("A model call is easy. Operating inference is the real problem.")`, `ui.button("Run Inference", on_click=run_request)`, `"Streaming response"`, `"Telemetry"`, `"Run history"`, `"Comparison"`; (2) the forbidden conversation-assistant vocabulary is absent — assert each forbidden term enumerated in <acceptance_criteria> is not in `text`, using the exact quoted-string forms listed there.

    Do NOT modify src/openrouter_demo/ui.py (DOC-04 is already satisfied; this guard only pins it). Choose the forbidden-vocabulary list so that every term is verifiably absent from ui.py — run the test after writing it and adjust the list only if a term produces a false positive (e.g., a term appearing in a comment or an unrelated identifier); the positive inference copy strings above are non-negotiable and must remain.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_ui_has_no_chatbot_labels" tests/test_ui.py` returns >= 1.
    - The test asserts presence of `ui.page_title("OpenRouter Production Inference Lab")` and `ui.button("Run Inference", on_click=run_request)` in `Path("src/openrouter_demo/ui.py").read_text()`.
    - The test asserts absence of the forbidden terms: the literal `ui.chat_message`, the double-quoted role strings `"assistant"` and `"user"`, and the UI-label terms `Chat` and `Send message`.
    - `git diff --exit-code src/openrouter_demo/ui.py` exits 0 (ui.py unchanged).
    - `uv run pytest tests/test_ui.py -q` reports all passed, 0 failed.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only addition; revert via git.</reversibility>
  <done>tests/test_ui.py pins the inference-operation framing and rejects the forbidden conversation-assistant vocabulary; src/openrouter_demo/ui.py is untouched and the UI test module passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add DOC-05 focused-test coverage guard to tests/test_docs.py</name>
  <files>tests/test_docs.py</files>
  <read_first>
    - tests/test_docs.py (created in Plan 06-01; append, do not modify existing functions)
    - tests/test_client.py (response/error focus area — note test_stream_401_raises_auth_error)
    - tests/test_routing.py (routing configuration — note test_strategies_dict_contains_three_selectable_strategies)
    - tests/test_telemetry.py (telemetry normalization — note test_telemetry_evidence_round_trip_preserves_sentinels)
    - tests/test_evals.py (eval scoring — note test_score_response_passes_and_fails)
    - tests/test_sqlite_store.py (telemetry persistence round-trip)
    - tests/test_ui.py (response/error rendering coverage)
    - .planning/phases/06-interview-walkthrough-and-quality-gates/06-RESEARCH.md (the DOC-05 focus-area mapping table)
  </read_first>
  <action>
    Add one test function to tests/test_docs.py: `def test_focused_test_coverage() -> None`. It pins the four DOC-05 focus areas to existing test files using the repo's Path(...).read_text() + plain-assert style. Assert each focus-area test file exists and contains at least one named test function: (1) response/error handling -> `Path("tests/test_client.py").exists()` and the substring `def test_stream_401_raises_auth_error` is in `Path("tests/test_client.py").read_text()`, plus `Path("tests/test_ui.py").exists()`; (2) routing configuration -> `Path("tests/test_routing.py").exists()` and the substring `def test_strategies_dict_contains_three_selectable_strategies` is in `Path("tests/test_routing.py").read_text()`; (3) telemetry normalization -> `Path("tests/test_telemetry.py").exists()` and the substring `def test_telemetry_evidence_round_trip_preserves_sentinels` is in `Path("tests/test_telemetry.py").read_text()`, plus `Path("tests/test_sqlite_store.py").exists()`; (4) eval scoring -> `Path("tests/test_evals.py").exists()` and the substring `def test_score_response_passes_and_fails` is in `Path("tests/test_evals.py").read_text()`.

    Do not add new production tests in this task — RESEARCH confirms all four focus areas are already covered. Only add a focused test if the coverage guard reveals a genuinely uncovered area (it should not).
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py tests/test_client.py tests/test_routing.py tests/test_telemetry.py tests/test_evals.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_focused_test_coverage" tests/test_docs.py` returns >= 1.
    - The test references all four focus-area files: tests/test_client.py, tests/test_routing.py, tests/test_telemetry.py, tests/test_evals.py, plus tests/test_sqlite_store.py and tests/test_ui.py.
    - The test asserts each of the named functions `test_stream_401_raises_auth_error`, `test_strategies_dict_contains_three_selectable_strategies`, `test_telemetry_evidence_round_trip_preserves_sentinels`, and `test_score_response_passes_and_fails` exists in its respective file's read_text().
    - `uv run pytest tests/test_docs.py tests/test_client.py tests/test_routing.py tests/test_telemetry.py tests/test_evals.py -q` reports all passed, 0 failed.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only addition; revert via git.</reversibility>
  <done>tests/test_docs.py pins all four DOC-05 focus areas to existing passing test files, and the focused test modules are confirmed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README.md -> git history | Human-written docs must never contain real API keys |
| docs -> reviewer | Reviewer-facing docs must not overstate implemented behavior |
| client.py -> Langfuse trace input | Trace input must never contain the API key |
| client.py -> OpenRouter | Live inference spend must stay bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure | README.md / docs/* / git | high | mitigate | `.env.example` keeps empty assignments only; never document real keys; guard tests assert trace input contains no API key |
| T-06-02 | DoS (self-inflicted) | eval prompts / live inference | medium | mitigate | small bounded default prompts + 5 checked-in eval cases + `request_timeout=60.0` |
| T-06-03 | Information disclosure | telemetry.py trace input | high | mitigate | existing `tests/test_evals.py` / `tests/test_telemetry.py` assert no API key in trace input |
| T-06-04 | Availability | telemetry.py `record_trace` | medium | mitigate | `record_trace` catches all exceptions, returns `failed`, never raises |

This phase installs zero new packages, so no package-legitimacy (T-06-SC) threat applies.
</threat_model>

<verification>
- `uv run pytest tests/test_ui.py -q` green.
- `uv run pytest tests/test_docs.py -q` green.
- `git diff --exit-code src/openrouter_demo/ui.py` confirms no UI source change.
</verification>

<success_criteria>
- DOC-04 is enforced: a guard rejects conversation-assistant labels and pins inference-operation copy, with ui.py unchanged.
- DOC-05 is enforced: a guard pins the four focus areas to existing passing test files.
</success_criteria>

<output>
Create `.planning/phases/06-interview-walkthrough-and-quality-gates/06-02-SUMMARY.md` when done
</output>
