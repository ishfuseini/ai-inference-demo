---
phase: 01-runnable-skeleton-and-config
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pyproject.toml
  - uv.lock
  - app.py
  - src/openrouter_demo/config.py
  - src/openrouter_demo/ui.py
  - tests/test_config.py
  - .env.example
  - README.md
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03, SETUP-04, SETUP-05]
user_setup:
  - service: openrouter
    why: "Required for later live inference; Phase 1 verifies setup readiness display only."
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard API keys"
  - service: langfuse
    why: "Optional tracing dependency; Phase 1 verifies disabled readiness display when credentials are absent."
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse project settings"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse project settings"
      - name: LANGFUSE_BASE_URL
        source: "Langfuse project settings"

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SETUP-01: `uv sync` installs the project environment from `pyproject.toml` and checked-in `uv.lock`."
    - "SETUP-02: `uv run python app.py` launches a local NiceGUI page."
    - "SETUP-03: `.env.example` documents `OPENROUTER_API_KEY` without secret values."
    - "SETUP-04: missing Langfuse env vars keep app launchable and show tracing disabled."
    - "SETUP-05: missing `OPENROUTER_API_KEY` shows setup guidance and does not attempt a live request."
    - "D-11 package versioning resolution: `pyproject.toml` uses conservative compatible lower bounds after Plan 01-01 approval, and `uv.lock` records exact resolved versions."
    - "D-03, D-04, D-05, D-06, D-07, D-08, D-10, D-11, and D-12 are implemented in the setup/config/launch slice."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python 3.12+ project metadata, runtime dependencies, dev dependency group, pytest config, Ruff config"
      exports: ["project.name", "project.requires-python", "project.dependencies", "dependency-groups.dev", "tool.pytest.ini_options", "tool.ruff"]
    - path: "uv.lock"
      provides: "Resolved dependency lock generated by uv"
    - path: "app.py"
      provides: "Reviewer launch entrypoint"
      exports: ["main"]
    - path: "src/openrouter_demo/config.py"
      provides: "Exported-env setup inspection"
      exports: ["OPENROUTER_API_KEY", "LANGFUSE_PUBLIC_KEY", "LANGFUSE_SECRET_KEY", "LANGFUSE_BASE_URL", "REQUIRED_ENV_VARS", "LANGFUSE_ENV_VARS", "AppConfig", "load_config"]
    - path: "src/openrouter_demo/ui.py"
      provides: "NiceGUI setup/status operations shell"
      exports: ["build_app"]
    - path: "tests/test_config.py"
      provides: "Config behavior tests"
    - path: ".env.example"
      provides: "Credential checklist"
    - path: "README.md"
      provides: "Minimal setup documentation"
  key_links:
    - from: "app.py"
      to: "src/openrouter_demo/config.py"
      via: "`main()` calls `load_config()` before rendering the UI"
      pattern: "load_config"
    - from: "app.py"
      to: "src/openrouter_demo/ui.py"
      via: "`main()` calls `build_app(config)` before `ui.run()`"
      pattern: "build_app"
    - from: "src/openrouter_demo/ui.py"
      to: "src/openrouter_demo/config.py"
      via: "`build_app(config: AppConfig)` renders OpenRouter and Langfuse readiness"
      pattern: "AppConfig"
  prohibitions:
    - "No live OpenRouter requests in Phase 1."
    - "No dotenv dependency or runtime dotenv parsing."
    - "No secrets or secret-like values in `.env.example`."
    - "No fake telemetry, routing, cache, eval, trace, model, provider, token, or cost values."
    - "No FastAPI product layer."
    - "No database."
---

<objective>
Create the Phase 1 tracer slice for install, exported-env inspection, and NiceGUI launch.

Purpose: Prove the reviewer can install dependencies, inspect credential readiness, and launch the local setup/status shell without live inference.
Output: `pyproject.toml`, `uv.lock`, `app.py`, `config.py`, `ui.py`, `.env.example`, `README.md`, and config tests.
</objective>

<execution_context>
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-and-config/01-PATTERNS.md
@.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
@docs/PRD.md
@docs/specs/quickstart.md
@docs/specs/acceptance-criteria.md
@docs/specs/contracts/local-demo-contract.md
@docs/ux/screen-spec.md
</context>

<phase_goal>
**As a** reviewer, **I want to** install dependencies, configure credentials, and launch a local NiceGUI shell, **so that** I can verify the repo is runnable without secrets in git.
</phase_goal>

<artifacts_this_phase_produces>
## Files

- `pyproject.toml`
- `uv.lock`
- `app.py`
- `src/openrouter_demo/config.py`
- `src/openrouter_demo/ui.py`
- `tests/test_config.py`
- `.env.example`
- `README.md`

## Symbols

- `app.main`
- `openrouter_demo.config.OPENROUTER_API_KEY`
- `openrouter_demo.config.LANGFUSE_PUBLIC_KEY`
- `openrouter_demo.config.LANGFUSE_SECRET_KEY`
- `openrouter_demo.config.LANGFUSE_BASE_URL`
- `openrouter_demo.config.REQUIRED_ENV_VARS`
- `openrouter_demo.config.LANGFUSE_ENV_VARS`
- `openrouter_demo.config.AppConfig`
- `openrouter_demo.config.load_config`
- `openrouter_demo.ui.build_app`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end setup status shell from env inspection to NiceGUI launch</name>
  <precondition>Plan 01-01 package checkpoint has an `approved` result in `.planning/phases/01-runnable-skeleton-and-config/01-01-SUMMARY.md`.</precondition>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md
    @.planning/phases/01-runnable-skeleton-and-config/01-PATTERNS.md
    @.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
    @docs/specs/quickstart.md
    @docs/ux/screen-spec.md
    @pyproject.toml (if it exists)
    @app.py (if it exists)
    @src/openrouter_demo/config.py (if it exists)
    @src/openrouter_demo/ui.py (if it exists)
    @tests/test_config.py (if it exists)
  </read_first>
  <behavior>
    - Test 1: `load_config({})` returns `openrouter_ready=False`, `missing_required=("OPENROUTER_API_KEY",)`, and `langfuse_ready=False`.
    - Test 2: `load_config({"OPENROUTER_API_KEY": "test-key"})` returns `openrouter_ready=True`, no missing required vars, and `langfuse_ready=False`.
    - Test 3: `load_config()` reads `os.environ` when no mapping is injected.
    - Test 4: complete Langfuse env vars set `langfuse_ready=True`; incomplete Langfuse env vars keep `langfuse_ready=False`.
  </behavior>
  <files>pyproject.toml, uv.lock, app.py, src/openrouter_demo/config.py, src/openrouter_demo/ui.py, tests/test_config.py</files>
  <action>Create the dependency and launch tracer per D-04, D-05, D-06, D-08, D-11, and D-12. Define `pyproject.toml` with project name `openrouter-production-inference-lab`, `requires-python = ">=3.12"`, runtime dependencies using conservative compatible lower bounds `nicegui>=3.16.0`, `httpx>=0.28.1`, and `langfuse>=4.14.4`, dev dependencies `pytest>=9.1.1` and `ruff>=0.16.3`, `[tool.pytest.ini_options] testpaths = ["tests"]`, and Ruff target `py312`. Generate `uv.lock` with uv after writing project metadata so exact resolved versions live in the lockfile. In `config.py`, define env-name constants, frozen dataclass `AppConfig`, and `load_config(environ: Mapping[str, str] | None = None) -> AppConfig` that reads exported env vars through `os.environ` only when no mapping is supplied and never returns secret values. In `ui.py`, define `build_app(config: AppConfig) -> None` with the title `OpenRouter Production Inference Lab`, subtitle `Route, observe, recover, and evaluate model calls.`, prompt textarea, sample prompt control, disabled `Run Inference` button, setup guidance for missing `OPENROUTER_API_KEY`, and a visible disabled Langfuse tracing state when optional vars are absent. In `app.py`, define `main() -> None` that loads config, calls `build_app(config)`, and starts NiceGUI with `ui.run(title="OpenRouter Production Inference Lab")` under the script guard.</action>
  <verify>
    <automated>uv sync</automated>
    <automated>uv run pytest tests/test_config.py -q</automated>
    <automated>uv run ruff check app.py src/openrouter_demo/config.py src/openrouter_demo/ui.py tests/test_config.py</automated>
    <automated>python3 -c "import subprocess, os, time, signal; env=os.environ.copy(); env.pop('OPENROUTER_API_KEY', None); p=subprocess.Popen(['uv','run','python','app.py'], env=env, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True); time.sleep(5); p.send_signal(signal.SIGINT); out=p.communicate(timeout=10)[0]; assert 'NiceGUI' in out or 'http://' in out or p.returncode in (0, -2, 130), out"</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` creates or updates `uv.lock`.
    - `uv run pytest tests/test_config.py -q` passes with missing, present, and incomplete env cases.
    - `uv run python app.py` starts the NiceGUI process without `OPENROUTER_API_KEY`.
    - UI setup state includes `OPENROUTER_API_KEY` guidance and Langfuse disabled readiness.
    - No code path in this task creates an OpenRouter request or Langfuse trace.
  </acceptance_criteria>
  <done>The reviewer launch path works from dependency sync to config inspection to a reachable NiceGUI setup/status shell without live external calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add credential checklist and setup-focused README</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @docs/specs/quickstart.md
    @docs/specs/acceptance-criteria.md
    @.env.example (if it exists)
    @README.md (if it exists)
    @tests/test_config.py
  </read_first>
  <behavior>
    - Test 1: `.env.example` contains the four documented env variable names and contains no assigned secret-like sample values.
    - Test 2: README contains `uv sync`, `uv run python app.py`, `OPENROUTER_API_KEY`, and the optional Langfuse env names.
  </behavior>
  <files>.env.example, README.md, tests/test_config.py</files>
  <action>Add `.env.example` and minimal setup docs per D-03, D-07, and D-10. `.env.example` must be a checklist with comments and empty assignments for `OPENROUTER_API_KEY`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, and `LANGFUSE_BASE_URL`. README must cover project purpose, prerequisites, `uv sync`, exported env configuration, `uv run python app.py`, missing OpenRouter key behavior, optional Langfuse disabled behavior, and clearly mark live inference/routing/telemetry/evals as out of phase for Phase 1. Extend `tests/test_config.py` with file-content assertions for `.env.example` and README setup commands.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` documents required OpenRouter and optional Langfuse variable names.
    - `.env.example` contains no committed key material and no non-empty sample assignment.
    - README setup path is enough for a reviewer to install, export env vars, and launch the shell.
    - README does not claim Phase 2-5 runtime capabilities are implemented.
  </acceptance_criteria>
  <done>Credential setup is documented in files and enforced by tests without committing secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Run full setup slice verification</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
    @pyproject.toml
    @app.py
    @src/openrouter_demo/config.py
    @src/openrouter_demo/ui.py
    @tests/test_config.py
    @README.md
    @.env.example
  </read_first>
  <files>pyproject.toml, uv.lock, app.py, src/openrouter_demo/config.py, src/openrouter_demo/ui.py, tests/test_config.py, .env.example, README.md</files>
  <action>Stabilize the setup/config/launch slice after Task 1 and Task 2 by fixing only failures from the Phase 1 validation commands. Keep changes scoped to install metadata, env readiness, setup shell rendering, and setup docs per D-09 and D-12.</action>
  <verify>
    <automated>uv sync</automated>
    <automated>uv run pytest tests/test_config.py -q</automated>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync`, focused config tests, and Ruff all pass from a clean checkout state.
    - Fixes do not add live inference, routing/fallback execution, eval execution, cache claims, or Langfuse trace creation.
    - The setup shell remains clean and understandable without high-fidelity visual polish.
  </acceptance_criteria>
  <done>Plan 01-02 leaves the setup/config/launch tracer ready for Plan 01-03 import-boundary expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell environment to Python config | Exported env vars enter application setup state. |
| Python config to NiceGUI UI | Credential readiness is displayed in a local browser page. |
| local filesystem to git | `.env.example`, README, project metadata, and lockfile are committed. |
| local browser to NiceGUI server | UI labels and prompt controls render locally without live request execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `.env.example` and README | high | mitigate | Use comments and empty assignments only; tests assert documented env names without committed key material. |
| T-01-02-02 | Information Disclosure | `src/openrouter_demo/config.py` | medium | mitigate | `load_config()` returns readiness booleans and missing env names, never secret values. |
| T-01-02-03 | Denial of Service | `app.py` startup | medium | mitigate | Missing `OPENROUTER_API_KEY` is modeled as setup state, not a startup exception. |
| T-01-02-04 | Tampering | `src/openrouter_demo/ui.py` rendering | medium | mitigate | Use NiceGUI text components for setup/status copy and avoid raw HTML rendering for user-controlled prompt content. |
| T-01-02-05 | Tampering | dependency resolution | high | mitigate | Plan depends on package-legitimacy approval from Plan 01-01 before `uv sync`. |
| T-01-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate + blocking human checkpoint for `[SUS]` PyPI packages in Plan 01-01. |
</threat_model>

<verification>
Overall checks for this plan: `uv sync`, `uv run pytest tests/test_config.py -q`, `uv run ruff check .`, and a bounded `uv run python app.py` launch smoke with `OPENROUTER_API_KEY` absent.
</verification>

<success_criteria>
Reviewer can install dependencies, launch the NiceGUI setup/status shell, understand required and optional credentials, and see missing-credential guidance without any live external API call.
</success_criteria>

<output>
Create `.planning/phases/01-runnable-skeleton-and-config/01-02-SUMMARY.md` when done.
</output>
