---
phase: 01-runnable-skeleton-and-config
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/openrouter_demo/__init__.py
  - src/openrouter_demo/client.py
  - src/openrouter_demo/routing.py
  - src/openrouter_demo/models.py
  - src/openrouter_demo/scenarios.py
  - src/openrouter_demo/telemetry.py
  - src/openrouter_demo/evals.py
  - evals/.gitkeep
  - tests/test_imports.py
  - tests/test_phase1_guards.py
autonomous: true
requirements: [SETUP-06]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SETUP-06: source layout separates UI, client, routing, scenarios, telemetry, evals, and typed models."
    - "D-01: full importable scaffold exists without reshaping the repo later."
    - "D-02: stubs are honest and importable, and do not pretend telemetry, routing, fallback, cache, eval, trace, or live inference exists."
    - "D-12: verification proves imports and non-live Phase 1 behavior only."
    - "SETUP-06 edge assumption resolved: importability is the Phase 1 verifier for package-boundary readiness."
  artifacts:
    - path: "src/openrouter_demo/__init__.py"
      provides: "Package marker and package metadata"
      exports: ["__all__", "__version__"]
    - path: "src/openrouter_demo/client.py"
      provides: "OpenRouter client ownership boundary"
      exports: ["PhaseNotImplementedError", "stream_chat_completion"]
    - path: "src/openrouter_demo/routing.py"
      provides: "Routing ownership boundary and strategy labels"
      exports: ["StrategyName", "ROUTING_STRATEGY_LABELS"]
    - path: "src/openrouter_demo/models.py"
      provides: "Typed model ownership boundary"
      exports: ["Unavailable", "UNAVAILABLE"]
    - path: "src/openrouter_demo/scenarios.py"
      provides: "Scenario ownership boundary"
      exports: ["PhaseNotImplementedError", "run_scenario"]
    - path: "src/openrouter_demo/telemetry.py"
      provides: "Telemetry ownership boundary"
      exports: ["TraceReadiness", "trace_readiness_from_config"]
    - path: "src/openrouter_demo/evals.py"
      provides: "Eval runner ownership boundary"
      exports: ["PhaseNotImplementedError", "main"]
    - path: "evals/.gitkeep"
      provides: "Checked-in evals directory without fake cases"
    - path: "tests/test_imports.py"
      provides: "Import-boundary tests"
    - path: "tests/test_phase1_guards.py"
      provides: "Non-live and honesty guard tests"
  key_links:
    - from: "tests/test_imports.py"
      to: "src/openrouter_demo/*.py"
      via: "Imports every required package module"
      pattern: "importlib.import_module"
    - from: "src/openrouter_demo/telemetry.py"
      to: "src/openrouter_demo/config.py"
      via: "`trace_readiness_from_config(config)` consumes `AppConfig.langfuse_ready`"
      pattern: "langfuse_ready"
    - from: "src/openrouter_demo/evals.py"
      to: "evals/.gitkeep"
      via: "Phase 1 keeps eval directory present without executable eval cases"
      pattern: "evals"
  prohibitions:
    - "No live OpenRouter requests in Phase 1."
    - "No dotenv dependency or runtime dotenv parsing."
    - "No secrets in `.env.example`."
    - "No fake telemetry, routing, cache, eval, trace, model, provider, token, or cost values."
    - "No FastAPI product layer."
    - "No database."
---

<objective>
Expand the tracer into the full importable package scaffold while preserving Phase 1 honesty.

Purpose: Later phases need stable module boundaries, but Phase 1 must only prove importability and setup readiness.
Output: `openrouter_demo` package modules for client, routing, scenarios, telemetry, evals, models, package metadata, evals directory marker, import tests, and non-live guard tests.
</objective>

<execution_context>
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishmusic/40-Workspace/demo/openrouter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-and-config/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-and-config/01-PATTERNS.md
@.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
@docs/PRD.md
@docs/specs/acceptance-criteria.md
@docs/specs/contracts/local-demo-contract.md
@docs/specs/data-model.md
@docs/ux/screen-spec.md
@src/openrouter_demo/config.py
@src/openrouter_demo/ui.py
</context>

<artifacts_this_phase_produces>
## Files

- `src/openrouter_demo/__init__.py`
- `src/openrouter_demo/client.py`
- `src/openrouter_demo/routing.py`
- `src/openrouter_demo/models.py`
- `src/openrouter_demo/scenarios.py`
- `src/openrouter_demo/telemetry.py`
- `src/openrouter_demo/evals.py`
- `evals/.gitkeep`
- `tests/test_imports.py`
- `tests/test_phase1_guards.py`

## Symbols

- `openrouter_demo.__all__`
- `openrouter_demo.__version__`
- `openrouter_demo.client.PhaseNotImplementedError`
- `openrouter_demo.client.stream_chat_completion`
- `openrouter_demo.routing.StrategyName`
- `openrouter_demo.routing.ROUTING_STRATEGY_LABELS`
- `openrouter_demo.models.Unavailable`
- `openrouter_demo.models.UNAVAILABLE`
- `openrouter_demo.scenarios.PhaseNotImplementedError`
- `openrouter_demo.scenarios.run_scenario`
- `openrouter_demo.telemetry.TraceReadiness`
- `openrouter_demo.telemetry.trace_readiness_from_config`
- `openrouter_demo.evals.PhaseNotImplementedError`
- `openrouter_demo.evals.main`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end import boundary for package metadata, client, routing, and typed models</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @.planning/phases/01-runnable-skeleton-and-config/01-PATTERNS.md
    @docs/PRD.md
    @docs/specs/data-model.md
    @src/openrouter_demo/__init__.py (if it exists)
    @src/openrouter_demo/client.py (if it exists)
    @src/openrouter_demo/routing.py (if it exists)
    @src/openrouter_demo/models.py (if it exists)
    @tests/test_imports.py (if it exists)
  </read_first>
  <behavior>
    - Test 1: `openrouter_demo`, `openrouter_demo.client`, `openrouter_demo.routing`, and `openrouter_demo.models` import without side effects.
    - Test 2: `stream_chat_completion()` raises `PhaseNotImplementedError` with a message that identifies live streaming as out of phase.
    - Test 3: routing labels include `default`, `cost`, `latency`, and `custom` without claiming provider behavior.
    - Test 4: unavailable metadata is represented by a named sentinel, not numeric zero.
  </behavior>
  <files>src/openrouter_demo/__init__.py, src/openrouter_demo/client.py, src/openrouter_demo/routing.py, src/openrouter_demo/models.py, tests/test_imports.py</files>
  <action>Create the first import-boundary slice per D-01 and D-02. Add package metadata in `__init__.py`. In `client.py`, define a `PhaseNotImplementedError` exception and `stream_chat_completion(*args: object, **kwargs: object) -> None` that raises it with clear Phase 2 ownership. In `routing.py`, define `StrategyName` using `Literal["default", "cost", "latency", "custom"]` and `ROUTING_STRATEGY_LABELS` with labels from `docs/ux/screen-spec.md`. In `models.py`, define a lightweight `Unavailable` dataclass or enum-style sentinel and `UNAVAILABLE` for later metadata honesty. Add `tests/test_imports.py` coverage for these modules and symbols.</action>
  <verify>
    <automated>uv run pytest tests/test_imports.py -q</automated>
    <automated>uv run ruff check src/openrouter_demo/__init__.py src/openrouter_demo/client.py src/openrouter_demo/routing.py src/openrouter_demo/models.py tests/test_imports.py</automated>
  </verify>
  <acceptance_criteria>
    - Package metadata, client, routing, and typed-model modules import cleanly.
    - Client boundary does not create network clients or send requests.
    - Routing labels are available for UI structure without claiming live route outcomes.
    - Metadata unavailable state is distinguishable from zero.
  </acceptance_criteria>
  <done>The first import scaffold slice is present and tested without live behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scenarios, telemetry, evals, and eval directory boundaries</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @.planning/phases/01-runnable-skeleton-and-config/01-PATTERNS.md
    @docs/specs/contracts/local-demo-contract.md
    @docs/specs/data-model.md
    @src/openrouter_demo/config.py
    @src/openrouter_demo/scenarios.py (if it exists)
    @src/openrouter_demo/telemetry.py (if it exists)
    @src/openrouter_demo/evals.py (if it exists)
    @tests/test_imports.py
  </read_first>
  <behavior>
    - Test 1: `openrouter_demo.scenarios`, `openrouter_demo.telemetry`, and `openrouter_demo.evals` import cleanly.
    - Test 2: scenario and eval execution functions raise `PhaseNotImplementedError` instead of producing fabricated output.
    - Test 3: `trace_readiness_from_config(load_config({}))` reports disabled tracing, while complete Langfuse env config reports enabled readiness without creating a trace.
    - Test 4: `evals/.gitkeep` exists and no Phase 1 eval cases are fabricated.
  </behavior>
  <files>src/openrouter_demo/scenarios.py, src/openrouter_demo/telemetry.py, src/openrouter_demo/evals.py, evals/.gitkeep, tests/test_imports.py</files>
  <action>Add the remaining package boundaries per D-01, D-02, D-06, and D-12. In `scenarios.py`, define `PhaseNotImplementedError` and `run_scenario(*args: object, **kwargs: object) -> None` that raises it with Phase 3 or Phase 4 ownership as appropriate to the requested scenario. In `telemetry.py`, define `TraceReadiness` and `trace_readiness_from_config(config: AppConfig) -> TraceReadiness` so optional Langfuse status is visible without trace creation. In `evals.py`, define `PhaseNotImplementedError` and `main() -> None` that raises it with Phase 5 ownership for the eval command contract. Create `evals/.gitkeep` so the directory exists without fabricated eval cases. Extend `tests/test_imports.py` to cover every required module.</action>
  <verify>
    <automated>uv run pytest tests/test_imports.py -q</automated>
    <automated>uv run ruff check src/openrouter_demo/scenarios.py src/openrouter_demo/telemetry.py src/openrouter_demo/evals.py tests/test_imports.py</automated>
  </verify>
  <acceptance_criteria>
    - Scenario, telemetry, and eval modules import cleanly and expose the planned ownership symbols.
    - Optional Langfuse readiness is derived from config and does not require credentials for app launch.
    - Eval directory exists without executable case data.
    - No module returns fake run, fallback, trace, cache, telemetry, or eval results.
  </acceptance_criteria>
  <done>All Phase 1 package boundaries and the eval directory are present, honest, and import-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add non-live guard tests and run full Phase 1 verification</name>
  <read_first>
    @.planning/phases/01-runnable-skeleton-and-config/01-CONTEXT.md
    @.planning/phases/01-runnable-skeleton-and-config/01-VALIDATION.md
    @docs/specs/contracts/local-demo-contract.md
    @src/openrouter_demo/client.py
    @src/openrouter_demo/routing.py
    @src/openrouter_demo/scenarios.py
    @src/openrouter_demo/telemetry.py
    @src/openrouter_demo/evals.py
    @tests/test_imports.py
    @tests/test_phase1_guards.py (if it exists)
  </read_first>
  <behavior>
    - Test 1: Phase 1 source under `app.py` and `src/openrouter_demo` contains no OpenRouter endpoint call construction.
    - Test 2: Phase 1 source imports no FastAPI product layer.
    - Test 3: Phase 1 source contains no database dependency imports.
    - Test 4: Phase 1 source does not use Langfuse trace creation APIs.
  </behavior>
  <files>tests/test_phase1_guards.py, src/openrouter_demo/client.py, src/openrouter_demo/scenarios.py, src/openrouter_demo/telemetry.py, src/openrouter_demo/evals.py</files>
  <action>Add `tests/test_phase1_guards.py` with source-inspection tests that enforce the Phase 1 boundary from D-02 and D-12. Scope the tests to `app.py` and `src/openrouter_demo` so docs can still discuss later phases. Check for absent live request construction, separate FastAPI product imports, database library imports, and Langfuse trace creation calls. Fix only failures caused by Phase 1 scaffold code, preserving the honest exception behavior in client, scenario, and eval boundaries.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py tests/test_imports.py tests/test_phase1_guards.py -q</automated>
    <automated>uv run pytest</automated>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 1 tests pass for config, importability, and non-live guards.
    - Full pytest and Ruff gates pass.
    - Guard tests are source-scoped to implementation files, not seed docs or planning artifacts.
    - Phase 1 still has no live inference, optional trace creation, eval execution, FastAPI product layer, or database integration.
  </acceptance_criteria>
  <done>Full Phase 1 scaffold verification passes with explicit non-live safeguards.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Reviewer can install dependencies, configure credentials, and launch a local NiceGUI shell without secrets in git. | 01-02 | COVERED | Tracer slice covers install, config, launch, and setup guidance. |
| REQ | SETUP-01 | Reviewer can install project dependencies with `uv sync`. | 01-01, 01-02 | COVERED | Package gate precedes dependency sync and lockfile generation. |
| REQ | SETUP-02 | Reviewer can launch the NiceGUI app with `uv run python app.py`. | 01-02 | COVERED | App entrypoint and launch smoke. |
| REQ | SETUP-03 | Reviewer can configure required `OPENROUTER_API_KEY` without committing secrets. | 01-02 | COVERED | `.env.example`, README, env-only config tests. |
| REQ | SETUP-04 | Reviewer can omit Langfuse credentials and still run the core inference demo. | 01-02, 01-03 | COVERED | Config and telemetry readiness report disabled optional tracing. |
| REQ | SETUP-05 | App shows setup guidance when required OpenRouter credential is missing. | 01-02 | COVERED | UI setup/status shell renders missing-key guidance. |
| REQ | SETUP-06 | Repository includes separated Python package layout. | 01-03 | COVERED | Importable modules for UI, client, routing, scenarios, telemetry, evals, models, config. |
| RESEARCH | - | Use `src/openrouter_demo` with thin `app.py`, env-only config, NiceGUI shell, `.env.example`, README, uv lock, tests. | 01-02, 01-03 | COVERED | Matches recommended project structure and validation architecture. |
| RESEARCH | - | Package Legitimacy Audit requires checkpoint for `[SUS]` packages before install. | 01-01 | COVERED | Blocking human package verification. |
| RESEARCH | - | No dotenv parsing; `os.environ` only. | 01-02 | COVERED | Config task and tests. |
| RESEARCH | - | Missing OpenRouter credential is setup state, not launch crash. | 01-02 | COVERED | Config and UI task. |
| RESEARCH | - | Honest stubs must not fake telemetry/routing/fallback/cache/eval/live behavior. | 01-03 | COVERED | Boundary modules and guard tests. |
| RESEARCH | - | Avoid FastAPI product layer and database scope. | 01-02, 01-03 | COVERED | Prohibitions and guard tests. |
| CONTEXT | D-01 | Create full importable scaffold and repo structure. | 01-02, 01-03 | COVERED | Setup files plus module boundaries. |
| CONTEXT | D-02 | Stubs honest and importable. | 01-03 | COVERED | PhaseNotImplementedError and guard tests. |
| CONTEXT | D-03 | `.env.example` useful as credential checklist. | 01-02 | COVERED | Credential checklist task. |
| CONTEXT | D-04 | Runtime config reads exported env vars only through `os.environ`. | 01-02 | COVERED | `load_config` contract. |
| CONTEXT | D-05 | Missing OpenRouter key launches setup/status shell and no live request. | 01-02, 01-03 | COVERED | UI setup shell and non-live guards. |
| CONTEXT | D-06 | Missing Langfuse credentials shown optional/disabled and non-blocking. | 01-02, 01-03 | COVERED | UI and telemetry readiness. |
| CONTEXT | D-07 | `.env.example` contains variable names/comments only. | 01-02 | COVERED | File-content tests. |
| CONTEXT | D-08 | First screen resembles inference operations console with empty/disabled panels. | 01-02 | COVERED | NiceGUI shell structure. |
| CONTEXT | D-09 | Avoid high-fidelity styling effort in Phase 1. | 01-02 | COVERED | Task 3 stabilization scope. |
| CONTEXT | D-10 | Add minimal README setup docs now. | 01-02 | COVERED | README setup task. |
| CONTEXT | D-11 | Include full planned dependency stack now. | 01-01, 01-02 | COVERED | Package gate and pyproject. |
| CONTEXT | D-12 | Verify setup/import/env/Ruff/launch only, no live request. | 01-02, 01-03 | COVERED | Validation commands and guard tests. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python imports to module initialization | Importing scaffold modules must not trigger live calls or trace creation. |
| local source to tests | Source-inspection guards enforce non-live Phase 1 boundaries. |
| future command contracts to current package | `python -m openrouter_demo.evals` ownership exists without executable eval behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | `client.py` and `scenarios.py` | high | mitigate | Stub execution raises `PhaseNotImplementedError`; guard tests reject live OpenRouter request construction in Phase 1 source. |
| T-01-03-02 | Information Disclosure | `telemetry.py` | medium | mitigate | Langfuse readiness is represented without trace creation or secret exposure. |
| T-01-03-03 | Tampering | `ui.py` and package modules | medium | mitigate | Guard tests reject separate FastAPI product imports and database imports in implementation source. |
| T-01-03-04 | Repudiation | eval command boundary | low | mitigate | `evals.py` raises a clear Phase 5 ownership exception instead of emitting fabricated scores. |
| T-01-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new install occurs in this plan; dependency install remains covered by Plan 01-01 package-legitimacy gate and Plan 01-02 lockfile generation. |
</threat_model>

<verification>
Overall checks for this plan: `uv run pytest tests/test_config.py tests/test_imports.py tests/test_phase1_guards.py -q`, `uv run pytest`, and `uv run ruff check .`.
</verification>

<success_criteria>
All Phase 1 package boundaries are present and importable; config, import, and non-live tests pass; no deferred live inference, routing/fallback, cache, eval, or tracing behavior is fabricated.
</success_criteria>

<output>
Create `.planning/phases/01-runnable-skeleton-and-config/01-03-SUMMARY.md` when done.
</output>
