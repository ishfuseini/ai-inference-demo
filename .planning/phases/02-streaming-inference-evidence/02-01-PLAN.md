---
phase: 02-streaming-inference-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/openrouter_demo/ui.py
  - tests/test_ui.py
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05, INF-06]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "INF-01: a non-empty prompt can start a live OpenRouter request through a UI-owned handler when an API key is available."
    - "INF-02: StreamChunk text deltas are appended progressively to accumulated response state."
    - "INF-03: StreamedResult model/provider evidence is copied into TelemetryEvidence when available and stays UNAVAILABLE when absent."
    - "INF-04: success/failure status and observed latency are recorded in InferenceRun/TelemetryEvidence."
    - "INF-05: token and cost fields are copied only when available."
    - "INF-06: missing token/cost/model/provider evidence never becomes 0, 0.0, empty string, None, or raw user-facing sentinel text."
    - "Phase 02 PR-1 backend is already present; this plan creates the testable UI/controller seam that consumes it."
  artifacts:
    - path: "src/openrouter_demo/ui.py"
      provides: "Testable streaming UI state helpers and metadata formatting"
      exports: ["_format_metadata", "_format_tokens", "_format_cost", "_run_inference"]
    - path: "tests/test_ui.py"
      provides: "Mocked UI streaming state tests"
      exports: []
  key_links:
    - from: "src/openrouter_demo/ui.py"
      to: "src/openrouter_demo/client.py"
      via: "Consumes stream_chat_completion-compatible async iterator"
      pattern: "StreamChunk | StreamedResult"
    - from: "src/openrouter_demo/ui.py"
      to: "src/openrouter_demo/history.py"
      via: "Appends InferenceRun to RunHistory"
      pattern: "history.append(run)"
  prohibitions:
    - "No live OpenRouter calls in tests."
    - "No Phase 3 strategy selection controls."
    - "No fallback, cache, eval, or Langfuse trace implementation in Phase 02."
    - "No separate frontend or FastAPI product layer."
---

<objective>
Create the Phase 02 tracer seam for streaming inference UI behavior: mocked stream input becomes progressive response state, terminal telemetry, and a run-history record.

Purpose: The NiceGUI surface can only be reliable if the streaming state transition is testable without a browser or live OpenRouter request.
Output: `src/openrouter_demo/ui.py` helpers plus `tests/test_ui.py` coverage for success, unavailable metadata, and mid-stream failure.
</objective>

<scope_note>
The backend streaming client, typed models, default routing strategy, and bounded run history already exist. Do not reimplement those. This plan builds the UI-owned orchestration seam that Phase 02 PR-2 needs.
</scope_note>

<execution_context>
@/Users/ishmusic/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ishmusic/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
@.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
@.planning/phases/02-streaming-inference-evidence/02-AI-SPEC.md
@.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
@.planning/phases/02-streaming-inference-evidence/02-VALIDATION.md
@docs/tasks/phase-2-streaming-inference.md
@docs/ux/screen-spec.md
@src/openrouter_demo/client.py
@src/openrouter_demo/models.py
@src/openrouter_demo/routing.py
@src/openrouter_demo/history.py
@src/openrouter_demo/ui.py
</context>

<artifacts_this_phase_produces>
## Files

- `src/openrouter_demo/ui.py`
- `tests/test_ui.py`

## Symbols

- `openrouter_demo.ui._format_metadata`
- `openrouter_demo.ui._format_tokens`
- `openrouter_demo.ui._format_cost`
- `openrouter_demo.ui._run_inference`
- `tests.test_ui`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add mocked UI streaming state tests</name>
  <read_first>
    @.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
    @.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-AI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
    @docs/tasks/phase-2-streaming-inference.md
    @docs/ux/screen-spec.md
    @src/openrouter_demo/client.py
    @src/openrouter_demo/models.py
    @src/openrouter_demo/routing.py
    @src/openrouter_demo/history.py
    @src/openrouter_demo/ui.py
    @tests/test_client.py
    @tests/test_ui.py (if it exists)
  </read_first>
  <behavior>
    - Test 1: a fake async stream yielding two `StreamChunk` objects and one `StreamedResult` records a succeeded `InferenceRun` with concatenated streamed text.
    - Test 2: a fake successful stream with `UNAVAILABLE` model/provider/token/cost fields leaves those fields as `UNAVAILABLE` in telemetry.
    - Test 3: a fake stream that yields a partial `StreamChunk` then raises `OpenRouterHTTPError(partial_text="partial")` records `Status.FAILED`, non-empty `error_message`, and `streamed_text == "partial"`.
    - Test 4: no test performs a live OpenRouter request; all stream behavior is injected.
  </behavior>
  <files>tests/test_ui.py</files>
  <action>Create `tests/test_ui.py` with focused tests for a UI-owned async handler named `_run_inference` (or an equivalent single handler symbol added in Task 2). Tests must import `RunHistory`, `Status`, `StreamChunk`, `StreamedResult`, `TelemetryEvidence`, `UNAVAILABLE`, `DEFAULT_STRATEGY`, and the UI handler/formatters from `src/openrouter_demo/ui.py`. Use fake async generator functions instead of `httpx.AsyncClient` for handler tests. Assert exact required copy for unavailable formatting: `Unavailable from selected route/provider.` and `Cost metadata was not returned for this route/provider.`</action>
  <verify>
    <automated>uv run pytest tests/test_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_ui.py` contains success, missing-metadata, and mid-stream-failure cases.
    - Tests use injected fake streams and do not instantiate a live network client.
    - Tests fail until `src/openrouter_demo/ui.py` exposes the planned handler/formatters.
  </acceptance_criteria>
  <done>UI streaming behavior has a failing/then-passing mocked contract before the NiceGUI surface is rewritten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UI state handler and metadata formatters</name>
  <read_first>
    @.planning/phases/02-streaming-inference-evidence/02-RESEARCH.md
    @.planning/phases/02-streaming-inference-evidence/02-UI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-AI-SPEC.md
    @.planning/phases/02-streaming-inference-evidence/02-PATTERNS.md
    @src/openrouter_demo/client.py
    @src/openrouter_demo/models.py
    @src/openrouter_demo/routing.py
    @src/openrouter_demo/history.py
    @src/openrouter_demo/ui.py
    @tests/test_ui.py
  </read_first>
  <behavior>
    - The handler starts from prompt text, API key, `DEFAULT_STRATEGY`, a `RunHistory`, and an injected stream function.
    - For each `StreamChunk`, the handler appends `text_delta` to accumulated response text.
    - On `StreamedResult`, the handler builds `TelemetryEvidence` and appends a `Status.SUCCEEDED` `InferenceRun`.
    - On `OpenRouterError`, the handler appends a `Status.FAILED` `InferenceRun` preserving `partial_text` when present.
  </behavior>
  <files>src/openrouter_demo/ui.py, tests/test_ui.py</files>
  <action>In `src/openrouter_demo/ui.py`, add formatting helpers `_format_metadata(value: str | Unavailable) -> str`, `_format_tokens(value: int | Unavailable) -> str`, and `_format_cost(value: float | Unavailable) -> str`. `_format_metadata` must return `Unavailable from selected route/provider.` for `UNAVAILABLE`; `_format_cost` must return `Cost metadata was not returned for this route/provider.` for `UNAVAILABLE`; numeric token/cost values must render as their actual value, not a guessed value. Add an async handler `_run_inference(prompt: str, *, api_key: str, history: RunHistory, stream_fn=stream_chat_completion, strategy: RoutingStrategy = DEFAULT_STRATEGY) -> InferenceRun`. The handler must reject blank prompts with `ValueError`, consume `StreamChunk` and `StreamedResult`, compute started/completed timestamps, create `TelemetryEvidence`, append the final `InferenceRun` to history, and return it for test assertions.</action>
  <verify>
    <automated>uv run pytest tests/test_ui.py tests/test_client.py -q</automated>
    <automated>uv run ruff check src/openrouter_demo/ui.py tests/test_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ui.py -q` passes.
    - `_run_inference` records success and failure runs in `RunHistory`.
    - Missing metadata remains `UNAVAILABLE` in `TelemetryEvidence` and renders through explicit UI copy helpers.
    - Partial text from typed OpenRouter errors is preserved in failed runs.
  </acceptance_criteria>
  <done>The UI/controller seam converts streamed client events into durable run evidence without live network in tests.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_ui.py tests/test_client.py -q` and `uv run ruff check src/openrouter_demo/ui.py tests/test_ui.py`.
</verification>

<success_criteria>
- `tests/test_ui.py` exists and proves success, missing metadata, and mid-stream failure.
- `src/openrouter_demo/ui.py` exposes testable streaming state helpers.
- No live OpenRouter call happens in tests.
- No unavailable metadata is coerced to zero or raw sentinel text.
</success_criteria>

<threat_model>
| Threat | Severity | Mitigation |
|---|---:|---|
| Tests accidentally call live OpenRouter and spend money or require credentials. | high | Use injected fake async streams only in `tests/test_ui.py`; no live client in tests. |
| UI state handler loses partial text on failure. | medium | Test OpenRouterError partial_text preservation into failed `InferenceRun`. |
| Missing metadata becomes zero or user-facing raw sentinel text. | medium | Test formatter output and telemetry sentinel preservation. |
</threat_model>
