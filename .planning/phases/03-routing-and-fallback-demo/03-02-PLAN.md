---
phase: 03-routing-and-fallback-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/openrouter_demo/scenarios.py
  - src/openrouter_demo/ui.py
  - tests/test_scenarios.py
  - tests/test_ui.py
  - tests/test_imports.py
autonomous: true
requirements:
  - ROUTE-04
  - ROUTE-05
  - ROUTE-06
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can toggle "Simulate primary route failure" and run inference to trigger a fallback scenario
    - Fallback scenario makes two attempts: primary (deterministic failure) then fallback (real strategy)
    - Primary attempt produces an AttemptRecord with status=FAILED and error_message from the HTTP error
    - Fallback attempt produces an AttemptRecord with status=SUCCEEDED and telemetry evidence
    - Successful fallback produces InferenceRun with status=FALLBACK_SUCCEEDED (not SUCCEEDED)
    - FallbackEvidence preserves both primary and fallback AttemptRecords on the InferenceRun
    - UI telemetry panel shows primary status, primary error, fallback model, and fallback status
    - UI response panel shows "Completed via fallback route after primary route failed." for fallback success
    - Run history shows "Yes" in the Fallback column for fallback runs
    - A successful fallback never hides the failed primary attempt — both are visible in telemetry
  artifacts:
    - src/openrouter_demo/scenarios.py contains run_fallback_scenario async generator and FallbackResult dataclass
    - src/openrouter_demo/ui.py contains _run_fallback_inference function, simulate_failure switch, fallback evidence rendering in _telemetry_rows
    - tests/test_scenarios.py exists with fallback scenario tests (primary fails, fallback succeeds, evidence preserved)
    - tests/test_ui.py extended with fallback handler and fallback evidence rendering tests
    - tests/test_imports.py updated to verify run_fallback_scenario import and PhaseNotImplementedError preservation
  key_links:
    - run_fallback_scenario uses stream_fn injection — testable without network
    - _run_fallback_inference consumes FallbackResult and builds InferenceRun(status=FALLBACK_SUCCEEDED, fallback_evidence=...)
    - _telemetry_rows renders fallback evidence rows when run.fallback_evidence is not None
    - run_request() branches: toggle ON → _run_fallback_inference, toggle OFF → _run_inference
---

<objective>
Fallback scenario vertical slice: user can trigger a reproducible fallback scenario that makes a deterministic primary failure (nonexistent model, allow_fallbacks=false), then a real fallback attempt, and preserves both attempts as visible evidence in the UI.

Purpose: ROUTE-04 (trigger reproducible fallback), ROUTE-05 (show primary failure, reason, fallback route, final result), ROUTE-06 (successful fallback does not hide primary failure). This builds on Plan 01's model types (AttemptRecord, FallbackEvidence, Status.FALLBACK_SUCCEEDED) and strategy registry (FALLBACK_PRIMARY_STRATEGY).

Output: Implemented run_fallback_scenario() in scenarios.py, _run_fallback_inference handler in ui.py, simulate_failure toggle in UI, fallback evidence rendering in telemetry panel, new tests/test_scenarios.py, extended tests/test_ui.py and tests/test_imports.py.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-and-fallback-demo/03-RESEARCH.md
@.planning/phases/03-routing-and-fallback-demo/03-PATTERNS.md
@.planning/phases/03-routing-and-fallback-demo/03-01-SUMMARY.md
@.planning/research/PITFALLS.md
@docs/ux/screen-spec.md
@docs/design/DESIGN-light.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fallback scenario orchestration — the two-attempt evidence engine</name>
  <files>src/openrouter_demo/scenarios.py, tests/test_scenarios.py</files>
  <read_first>
    - src/openrouter_demo/scenarios.py (current stub — PhaseNotImplementedError class and run_scenario function)
    - src/openrouter_demo/client.py (stream_chat_completion signature, OpenRouterError/OpenRouterHTTPError types, how errors carry status_code and partial_text)
    - src/openrouter_demo/models.py (after Plan 01 — AttemptRecord, FallbackEvidence, Status.FALLBACK_SUCCEEDED, StreamChunk, StreamedResult, UNAVAILABLE)
    - src/openrouter_demo/routing.py (after Plan 01 — FALLBACK_PRIMARY_STRATEGY, RoutingStrategy)
    - src/openrouter_demo/ui.py lines 148-210 (_run_inference pattern — how stream events are consumed, how InferenceRun is assembled, how OpenRouterError is caught)
    - tests/test_ui.py lines 17-40 (fake_stream injected async stream pattern — how to inject streams that yield chunks and results)
    - tests/test_ui.py lines 73-76 (error injection pattern — how to inject a stream that raises OpenRouterHTTPError)
    - .planning/phases/03-routing-and-fallback-demo/03-RESEARCH.md lines 330-400 (run_fallback_scenario code example, FallbackResult dataclass)
    - .planning/phases/03-routing-and-fallback-demo/03-PATTERNS.md lines 150-210 (scenarios.py pattern assignment — signature, imports, dual-stream test pattern)
    - .planning/research/PITFALLS.md (Pitfall 2: Silent or Hidden Fallback — both attempts must be visible)
  </read_first>
  <behavior>
    - Test: run_fallback_scenario with a failing primary stream (raises OpenRouterHTTPError status_code=404) and a succeeding fallback stream (yields StreamChunk + StreamedResult) produces a FallbackResult where primary.status is Status.FAILED and primary.error_message contains "404"
    - Test: FallbackResult.primary.model == "nonexistent/fake-model-for-demo" (from FALLBACK_PRIMARY_STRATEGY)
    - Test: FallbackResult.primary.provider is UNAVAILABLE (no provider returned on a failed request)
    - Test: FallbackResult.fallback is a StreamedResult with model="openai/gpt-4o-mini" and provider="OpenAI"
    - Test: FallbackResult.simulated is True
    - Test: Stream chunks from the fallback attempt are yielded progressively (collect them and assert order: "Fallback " then "response")
    - Test: primary.latency_ms is a non-negative int
    - Test: When primary unexpectedly succeeds (stream yields a StreamedResult instead of raising), the function handles it gracefully — yields a FallbackResult with primary.status=SUCCEEDED
  </behavior>
  <action>
Implement run_fallback_scenario() in scenarios.py, replacing the run_scenario stub function. KEEP the PhaseNotImplementedError class — test_imports.py still imports it and evals.py has its own copy.

Add FallbackResult frozen dataclass to scenarios.py with fields: primary (AttemptRecord), fallback (StreamedResult | None), simulated (bool). Import AttemptRecord, Status from models.

Add these imports to scenarios.py:
- import time
- from collections.abc import AsyncIterator, Callable
- from dataclasses import dataclass
- from openrouter_demo.client import OpenRouterError, stream_chat_completion
- from openrouter_demo.models import AttemptRecord, Status, StreamChunk, StreamedResult, UNAVAILABLE
- from openrouter_demo.routing import FALLBACK_PRIMARY_STRATEGY, RoutingStrategy

Define a StreamFn type alias: type StreamFn = Callable[..., AsyncIterator[StreamChunk | StreamedResult]] (same as ui.py's pattern).

Implement run_fallback_scenario with this signature:
async def run_fallback_scenario(prompt: str, *, fallback_strategy: RoutingStrategy, api_key: str, stream_fn: StreamFn = stream_chat_completion) -> AsyncIterator[StreamChunk | FallbackResult]

Logic:
1. Primary attempt: call stream_fn(prompt, strategy=FALLBACK_PRIMARY_STRATEGY, api_key=api_key). Consume the entire stream (iterate through it). Use time.monotonic() for latency measurement (same pattern as client.py line 99). Catch OpenRouterError — store the exception. If no exception is raised (primary unexpectedly succeeded), set primary_error to None.
2. Build primary_record as AttemptRecord: model=FALLBACK_PRIMARY_STRATEGY.model, provider=UNAVAILABLE, status=Status.FAILED if primary_error is not None else Status.SUCCEEDED, error_message=str(primary_error) if primary_error else None, latency_ms=int((time.monotonic() - primary_start) * 1000), prompt_tokens=UNAVAILABLE, completion_tokens=UNAVAILABLE, total_tokens=UNAVAILABLE, cost_usd=UNAVAILABLE.
3. If primary unexpectedly succeeded (primary_error is None): yield FallbackResult(primary=primary_record, fallback=None, simulated=True) and return early. Do NOT make a fallback attempt.
4. Fallback attempt: call stream_fn(prompt, strategy=fallback_strategy, api_key=api_key). Iterate through events. For each StreamChunk, yield it (so UI can display progressive text). For StreamedResult, store it as fallback_result. Do NOT catch OpenRouterError here — if the fallback also fails, let the exception propagate (the UI handler will catch it).
5. Yield FallbackResult(primary=primary_record, fallback=fallback_result, simulated=True).

Remove the run_scenario stub function. The PhaseNotImplementedError class stays.

Create tests/test_scenarios.py following test_ui.py injected-stream patterns:
- test_fallback_scenario_primary_fails_fallback_succeeds: use a dual-stream that raises OpenRouterHTTPError("Model not found", status_code=404, partial_text="") on first call, yields StreamChunk("Fallback ") + StreamChunk("response") + StreamedResult(text="Fallback response", model="openai/gpt-4o-mini", provider="OpenAI", prompt_tokens=3, completion_tokens=4, total_tokens=7, cost_usd=0.001, latency_ms=200) on second call. Collect all yielded events. Assert: last event is FallbackResult, result.primary.status is Status.FAILED, "404" in result.primary.error_message, result.primary.model == "nonexistent/fake-model-for-demo", result.primary.provider is UNAVAILABLE, result.fallback is not None, result.fallback.model == "openai/gpt-4o-mini", result.fallback.provider == "OpenAI", result.simulated is True.
- test_fallback_scenario_yields_stream_chunks_progressively: same dual-stream, collect StreamChunk events before the FallbackResult, assert len(chunks) == 2 and chunks[0].text_delta == "Fallback " and chunks[1].text_delta == "response".
- test_fallback_scenario_primary_latency_is_non_negative: assert result.primary.latency_ms >= 0.
- test_fallback_scenario_primary_unexpectedly_succeeds: inject a stream that yields a StreamedResult on the first call (no error). Assert FallbackResult.primary.status is Status.SUCCEEDED and FallbackResult.fallback is None.

Use a call_count counter in the dual_stream function to distinguish primary vs fallback calls (same pattern as PATTERNS.md dual_stream example):
call_count = 0; async def dual_stream(*args, **kwargs): nonlocal call_count; call_count += 1; if call_count == 1: raise OpenRouterHTTPError(...); yield StreamChunk(...); yield StreamedResult(...)

Use asyncio.run() to consume the async generator: collect events into a list via [event async for event in run_fallback_scenario(...)].
  </action>
  <verify>
    <automated>uv run pytest tests/test_scenarios.py -q</automated>
  </verify>
  <done>
    - run_fallback_scenario is an async generator that yields StreamChunk events then a terminal FallbackResult
    - Primary attempt uses FALLBACK_PRIMARY_STRATEGY and catches OpenRouterError
    - Primary AttemptRecord has status=FAILED, error_message from the exception, model from FALLBACK_PRIMARY_STRATEGY
    - Fallback attempt uses the selected fallback_strategy and yields StreamChunk events progressively
    - FallbackResult.simulated is True
    - Primary unexpected success edge case handled gracefully (yields FallbackResult with primary.status=SUCCEEDED, fallback=None)
    - PhaseNotImplementedError class preserved in scenarios.py
    - run_scenario stub function removed
    - All test_scenarios.py tests pass
  </done>
  <acceptance_criteria>
    - src/openrouter_demo/scenarios.py contains async def run_fallback_scenario
    - src/openrouter_demo/scenarios.py contains class FallbackResult with @dataclass(frozen=True)
    - src/openrouter_demo/scenarios.py contains FALLBACK_PRIMARY_STRATEGY in imports
    - src/openrouter_demo/scenarios.py contains OpenRouterError in imports
    - src/openrouter_demo/scenarios.py contains time.monotonic
    - src/openrouter_demo/scenarios.py does NOT contain def run_scenario (stub removed)
    - src/openrouter_demo/scenarios.py DOES contain class PhaseNotImplementedError (preserved)
    - tests/test_scenarios.py exits 0
    - tests/test_scenarios.py contains test_fallback_scenario_primary_fails_fallback_succeeds
    - tests/test_scenarios.py contains test_fallback_scenario_yields_stream_chunks_progressively
    - tests/test_scenarios.py contains test_fallback_scenario_primary_unexpectedly_succeeds
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI fallback handler, toggle, and evidence rendering — wire the scenario to the console</name>
  <files>src/openrouter_demo/ui.py, tests/test_ui.py, tests/test_imports.py</files>
  <read_first>
    - src/openrouter_demo/ui.py (after Plan 01 — strategy_select, run_request, _run_inference, _telemetry_rows with FALLBACK_SUCCESS_RESPONSE, _history_rows with Fallback column, build_app)
    - src/openrouter_demo/scenarios.py (after Task 1 — run_fallback_scenario signature, FallbackResult dataclass)
    - src/openrouter_demo/models.py (after Plan 01 — AttemptRecord, FallbackEvidence, Status.FALLBACK_SUCCEEDED, TelemetryEvidence)
    - tests/test_ui.py (after Plan 01 — existing test patterns, fake_stream, _run_inference tests, _telemetry_rows tests, _history_rows tests)
    - tests/test_imports.py (current — test_live_boundaries_raise_honest_phase_errors with run_scenario assertion)
    - docs/ux/screen-spec.md lines 160-180 (failure simulation toggle: "Simulate primary route failure", helper text "For a reproducible demo. The UI will label this as simulated.")
    - docs/ux/screen-spec.md lines 230-260 (telemetry panel fallback fields: primary status, primary error, fallback model, fallback status, failure type)
    - .planning/phases/03-routing-and-fallback-demo/03-RESEARCH.md lines 650-690 (telemetry rows with fallback, UI strategy selector and fallback toggle code example)
    - .planning/research/PITFALLS.md (Pitfall 2: Silent or Hidden Fallback — both attempts must be visible; Pitfall 1: Claiming Metadata — UNAVAILABLE must stay sentinel)
  </read_first>
  <behavior>
    - Test: _run_fallback_inference with a dual-stream (primary fails with 404, fallback succeeds) produces InferenceRun with status=Status.FALLBACK_SUCCEEDED
    - Test: InferenceRun.fallback_evidence is not None and has primary.status=FAILED and fallback.status=SUCCEEDED
    - Test: InferenceRun.streamed_text == "Fallback response" (text from fallback attempt)
    - Test: InferenceRun.telemetry has model="openai/gpt-4o-mini" and provider="OpenAI" (from fallback StreamedResult)
    - Test: _telemetry_rows with a FALLBACK_SUCCEEDED run shows "Primary status" row with "failed", "Primary error" row with the error message, "Fallback model" row with the model, "Fallback status" row with "succeeded"
    - Test: _telemetry_rows with a FALLBACK_SUCCEEDED run shows Status row as "Completed via fallback route after primary route failed."
    - Test: run_fallback_scenario is importable from openrouter_demo.scenarios
    - Test: test_live_boundaries_raise_honest_phase_errors no longer asserts run_scenario raises (stub removed) — instead asserts run_fallback_scenario is callable
  </behavior>
  <action>
Update ui.py to wire the fallback scenario into the UI:

1. Update imports: add run_fallback_scenario, FallbackResult from scenarios; add AttemptRecord, FallbackEvidence from models; add FALLBACK_PRIMARY_STRATEGY from routing (if not already imported in Plan 01). Add a type alias for the fallback stream function if needed.

2. Add _run_fallback_inference async function with this signature:
async def _run_fallback_inference(prompt: str, *, api_key: str, history: RunHistory, fallback_strategy: RoutingStrategy, stream_fn: StreamFn = stream_chat_completion) -> InferenceRun

Logic:
- started_at = datetime.now(UTC)
- text_parts: list[str] = []
- fallback_result: StreamedResult | None = None
- primary_record: AttemptRecord | None = None
- Iterate through run_fallback_scenario(prompt, fallback_strategy=fallback_strategy, api_key=api_key, stream_fn=stream_fn):
  - If StreamChunk: append to text_parts (do NOT yield — UI handles progressive display via observed_stream wrapper)
  - If FallbackResult: store primary_record = result.primary, fallback_result = result.fallback
- After iteration, build telemetry from fallback_result if it's not None: TelemetryEvidence(model=fallback_result.model, provider=fallback_result.provider, latency_ms=fallback_result.latency_ms, prompt_tokens=fallback_result.prompt_tokens, completion_tokens=fallback_result.completion_tokens, total_tokens=fallback_result.total_tokens, cost_usd=fallback_result.cost_usd)
- Build fallback_attempt_record from fallback_result: AttemptRecord(model=fallback_result.model, provider=fallback_result.provider, status=Status.SUCCEEDED, error_message=None, latency_ms=fallback_result.latency_ms, prompt_tokens=fallback_result.prompt_tokens, completion_tokens=fallback_result.completion_tokens, total_tokens=fallback_result.total_tokens, cost_usd=fallback_result.cost_usd)
- Build FallbackEvidence(primary=primary_record, fallback=fallback_attempt_record, simulated=True)
- Build InferenceRun(status=Status.FALLBACK_SUCCEEDED, streamed_text=fallback_result.text or "".join(text_parts), error_message=None, telemetry=telemetry, fallback_evidence=fallback_evidence, strategy_name=fallback_strategy.name)
- history.append(run), return run
- If fallback_result is None (primary unexpectedly succeeded): build InferenceRun with status=Status.SUCCEEDED, no fallback_evidence, telemetry=None, error_message=None. This is the edge case — treat as normal run.

3. Update _telemetry_rows to render fallback evidence when run.fallback_evidence is not None. After the existing rows, add:
- ("Primary status", fe.primary.status.value)
- ("Primary error", fe.primary.error_message or _UNAVAILABLE_COPY)
- ("Fallback model", _format_metadata(fe.fallback.model))
- ("Fallback status", fe.fallback.status.value)
- If fe.simulated: ("Failure type", "Simulated failure triggered for demo.")
Use _format_metadata for model/provider values — never stringify UNAVAILABLE directly. For the status row, use FALLBACK_SUCCESS_RESPONSE when run.status is Status.FALLBACK_SUCCEEDED (already added in Plan 01).

4. In build_app, add a simulate_failure switch in the prompt/strategy card (after the strategy selector and description):
- simulate_failure = ui.switch("Simulate primary route failure", value=False)
- ui.label("For a reproducible demo. The UI will label this as simulated.").classes("text-sm text-gray-600")

5. In run_request(), add branching before calling _run_inference:
- Read selected_strategy = STRATEGIES.get(strategy_select.value, DEFAULT_STRATEGY)
- If simulate_failure.value: call _run_fallback_inference instead of _run_inference, passing fallback_strategy=selected_strategy, stream_fn=observed_stream. The observed_stream wrapper needs to handle FallbackResult events — it should NOT try to display them as text; it should pass them through. Update observed_stream to handle FallbackResult: if event is not StreamChunk and not StreamedResult, just yield it without UI update.
- Update state.response_status: if run.status is Status.FALLBACK_SUCCEEDED, set state.response_status = FALLBACK_SUCCESS_RESPONSE (already handled by existing logic if it checks Status.SUCCEEDED — but FALLBACK_SUCCEEDED is a different enum value, so add an explicit check: response_status = FALLBACK_SUCCESS_RESPONSE if run.status is Status.FALLBACK_SUCCEEDED else (SUCCESS_RESPONSE if run.status is Status.SUCCEEDED else FAILURE_RESPONSE))

Update tests/test_ui.py:
- Add test_run_fallback_inference_produces_fallback_succeeded_run: inject a dual-stream (call_count pattern: first call raises OpenRouterHTTPError("Model not found", status_code=404, partial_text=""), second call yields StreamChunk("Fallback ") + StreamChunk("response") + StreamedResult(text="Fallback response", model="openai/gpt-4o-mini", provider="OpenAI", prompt_tokens=3, completion_tokens=4, total_tokens=7, cost_usd=0.001, latency_ms=200)). Call _run_fallback_inference(prompt="test", api_key="sk-test", history=RunHistory(), fallback_strategy=DEFAULT_STRATEGY, stream_fn=dual_stream). Assert: run.status is Status.FALLBACK_SUCCEEDED, run.fallback_evidence is not None, run.fallback_evidence.primary.status is Status.FAILED, run.fallback_evidence.fallback.status is Status.SUCCEEDED, run.streamed_text == "Fallback response", run.telemetry is not None, run.telemetry.model == "openai/gpt-4o-mini".
- Add test_telemetry_rows_render_fallback_evidence: create an InferenceRun with status=FALLBACK_SUCCEEDED, telemetry, and fallback_evidence with primary(FAILED, error="OpenRouter request failed (404)") and fallback(SUCCEEDED, model="openai/gpt-4o-mini"). Assert _telemetry_rows(run) contains ("Primary status", "failed"), ("Primary error", "OpenRouter request failed (404)"), ("Fallback model", "openai/gpt-4o-mini"), ("Fallback status", "succeeded"), ("Failure type", "Simulated failure triggered for demo."). Assert the Status row is FALLBACK_SUCCESS_RESPONSE.
- Add test_run_fallback_inference_appends_to_history: assert history.all() == [run] after _run_fallback_inference.

Update tests/test_imports.py:
- In test_live_boundaries_raise_honest_phase_errors: remove the run_scenario() assertion (the stub is removed). Keep the evals PhaseNotImplementedError assertion. Add: from openrouter_demo.scenarios import run_fallback_scenario; assert callable(run_fallback_scenario). Keep the ScenarioNotImplemented import (PhaseNotImplementedError class is preserved).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ui.py tests/test_imports.py tests/test_scenarios.py -q</automated>
  </verify>
  <done>
    - _run_fallback_inference consumes run_fallback_scenario events and builds InferenceRun with status=FALLBACK_SUCCEEDED
    - FallbackEvidence on InferenceRun preserves both primary (FAILED) and fallback (SUCCEEDED) AttemptRecords
    - _telemetry_rows renders primary status, primary error, fallback model, fallback status, and failure type rows when fallback_evidence is present
    - simulate_failure ui.switch added to the prompt/strategy card
    - run_request() branches to _run_fallback_inference when toggle is ON
    - observed_stream handles FallbackResult events without crashing
    - response_status uses FALLBACK_SUCCESS_RESPONSE for FALLBACK_SUCCEEDED runs
    - test_imports.py updated: run_scenario stub assertion removed, run_fallback_scenario import verified
    - All new and existing tests pass
  </done>
  <acceptance_criteria>
    - src/openrouter_demo/ui.py contains async def _run_fallback_inference
    - src/openrouter_demo/ui.py contains simulate_failure = ui.switch
    - src/openrouter_demo/ui.py contains run_fallback_scenario in imports
    - src/openrouter_demo/ui.py contains FallbackResult in imports or isinstance check
    - src/openrouter_demo/ui.py _telemetry_rows function contains "Primary status" and "Fallback model" string literals
    - src/openrouter_demo/ui.py _telemetry_rows function contains "Simulated failure triggered for demo."
    - src/openrouter_demo/ui.py run_request contains simulate_failure.value check
    - tests/test_ui.py contains test_run_fallback_inference_produces_fallback_succeeded_run
    - tests/test_ui.py contains test_telemetry_rows_render_fallback_evidence
    - tests/test_imports.py contains run_fallback_scenario import assertion
    - tests/test_imports.py does NOT contain run_scenario() call (stub removed)
    - uv run pytest tests/test_ui.py tests/test_imports.py tests/test_scenarios.py -q exits 0
    - uv run ruff check src/openrouter_demo/ui.py src/openrouter_demo/scenarios.py exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full suite verification and ruff gate</name>
  <files></files>
  <read_first>
    - pyproject.toml (pytest and ruff configuration)
  </read_first>
  <action>
Run the full test suite and lint gate to verify no regressions from Task 1 and Task 2 changes and that all Phase 3 requirements are covered by passing tests.

Run: uv run pytest tests/ -v
Run: uv run ruff check .
Run: uv run ruff format --check .

If any test fails: read the failure output, identify the cause, and fix. If ruff reports formatting issues, run uv run ruff format src/openrouter_demo/scenarios.py src/openrouter_demo/ui.py tests/test_scenarios.py tests/test_ui.py tests/test_imports.py to auto-format.

Verify all ROUTE requirements are covered:
- ROUTE-01: test_routing.py asserts strategy payloads for default/cost/latency
- ROUTE-02: test_ui.py asserts strategy description label updates (from Plan 01 Task 2)
- ROUTE-03: test_ui.py asserts run.strategy_name matches selected strategy
- ROUTE-04: test_scenarios.py asserts run_fallback_scenario produces FallbackResult
- ROUTE-05: test_scenarios.py asserts primary AttemptRecord has FAILED status and error_message; test_ui.py asserts telemetry rows show primary failure and fallback success
- ROUTE-06: test_ui.py asserts InferenceRun.fallback_evidence preserves both attempts; _telemetry_rows renders both

If any requirement is not covered by a passing test, add the missing test assertion before completing.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <done>
    - uv run pytest tests/ -q exits 0 (all tests pass including Phase 3 tests)
    - uv run ruff check . exits 0 (no lint errors)
    - uv run ruff format --check . exits 0 (no formatting issues)
    - All six ROUTE requirements (ROUTE-01 through ROUTE-06) have passing test coverage
  </done>
  <acceptance_criteria>
    - uv run pytest tests/ -q exits 0
    - uv run ruff check . exits 0
    - uv run ruff format --check . exits 0
    - uv run pytest tests/test_routing.py tests/test_scenarios.py tests/test_ui.py tests/test_imports.py -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | User toggles "Simulate primary route failure" switch — boolean state, no free-text injection |
| UI → scenarios.py | _run_fallback_inference calls run_fallback_scenario with selected strategy — internal Python call, no untrusted input |
| scenarios.py → OpenRouter API | Two sequential HTTP calls: primary (nonexistent model, allow_fallbacks=false) then fallback (real strategy) — crosses network |
| OpenRouter → client.py | SSE responses for both attempts — primary returns 404 error, fallback returns streaming success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | Fallback primary model slug | low | mitigate | FALLBACK_PRIMARY_STRATEGY uses hardcoded nonexistent model "nonexistent/fake-model-for-demo" — user cannot inject arbitrary model names. Strategy is not in STRATEGIES dict, not user-selectable. |
| T-03-06 | Information Disclosure | Primary failure error message | low | accept | Error message from OpenRouterHTTPError is displayed in telemetry ("Primary error" row). This is operational metadata, not sensitive data. Error messages from OpenRouter do not contain credentials. |
| T-03-07 | Denial of Service | Double API call per fallback run | low | accept | Primary attempt fails immediately with 404 (zero tokens, zero cost). Only fallback attempt generates cost. Total cost per fallback run equals one normal run. Research verified: "Requests are priced using the model that was ultimately used." |
| T-03-08 | Spoofing | Fallback evidence fabrication | medium | mitigate | AttemptRecord is a frozen dataclass built from actual OpenRouterError exceptions and StreamedResult — cannot be constructed with fabricated data by the UI. _run_fallback_inference builds evidence from real stream events, not user input. |
| T-03-SC | Tampering | No new pip packages | high | accept | No new packages introduced. All dependencies existing from Phase 1/2, already verified in Package Legitimacy Audit. |
</threat_model>

<verification>
- `uv run pytest tests/test_scenarios.py -q` — fallback scenario orchestration (primary fails, fallback succeeds, evidence preserved, progressive chunks)
- `uv run pytest tests/test_ui.py tests/test_imports.py -q` — fallback handler, telemetry evidence rendering, import verification
- `uv run pytest tests/ -q && uv run ruff check . && uv run ruff format --check .` — full suite and lint gate
- Manual: launch `uv run python app.py`, toggle "Simulate primary route failure", run inference, verify both primary failure and fallback success visible in telemetry panel
</verification>

<success_criteria>
1. User can toggle "Simulate primary route failure" and run inference to trigger the fallback scenario (ROUTE-04)
2. Fallback scenario shows the failed primary attempt with failure reason (404 error message) in telemetry (ROUTE-05)
3. Fallback scenario shows the fallback route (model/provider) and final result (streamed text) in telemetry (ROUTE-05)
4. Successful fallback produces status=FALLBACK_SUCCEEDED, not SUCCEEDED — primary failure is never hidden (ROUTE-06)
5. FallbackEvidence preserves both AttemptRecords on the InferenceRun — both visible in telemetry rows (ROUTE-06)
6. Run history shows "Yes" in the Fallback column for fallback runs (ROUTE-05)
7. All existing tests pass — no regressions from Plan 01 or Plan 02
8. No new packages required — existing stack sufficient
</success_criteria>

<output>
Create `.planning/phases/03-routing-and-fallback-demo/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Type | Purpose |
|--------|------|------|---------|
| `run_fallback_scenario()` | `src/openrouter_demo/scenarios.py` | async generator | Two-attempt fallback orchestration: primary (deterministic failure) then fallback (real strategy), yields StreamChunk events then terminal FallbackResult |
| `FallbackResult` | `src/openrouter_demo/scenarios.py` | frozen dataclass | Terminal event from fallback scenario: primary AttemptRecord + fallback StreamedResult + simulated flag |
| `_run_fallback_inference()` | `src/openrouter_demo/ui.py` | async function | Consumes run_fallback_scenario events, builds InferenceRun(status=FALLBACK_SUCCEEDED, fallback_evidence=...) |
| `simulate_failure` | `src/openrouter_demo/ui.py` | NiceGUI ui.switch | "Simulate primary route failure" toggle — routes run through fallback scenario when ON |
| Fallback evidence rows in `_telemetry_rows` | `src/openrouter_demo/ui.py` | function extension | Renders Primary status, Primary error, Fallback model, Fallback status, Failure type rows when fallback_evidence is present |
| `test_scenarios.py` | `tests/test_scenarios.py` | test file | Fallback scenario tests: primary fails/fallback succeeds, progressive chunks, primary unexpected success edge case |
| `test_run_fallback_inference_produces_fallback_succeeded_run` | `tests/test_ui.py` | test function | Verifies _run_fallback_inference produces FALLBACK_SUCCEEDED with both attempt records |
| `test_telemetry_rows_render_fallback_evidence` | `tests/test_ui.py` | test function | Verifies telemetry rows show primary failure and fallback success evidence |
| Updated `test_live_boundaries_raise_honest_phase_errors` | `tests/test_imports.py` | test function | Updated to verify run_fallback_scenario is callable instead of run_scenario stub raising |