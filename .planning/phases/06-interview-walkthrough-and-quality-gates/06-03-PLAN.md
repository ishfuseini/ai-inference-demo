---
phase: 06-interview-walkthrough-and-quality-gates
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: true
requirements: [DOC-06, DOC-07, DOC-08]

estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`uv run pytest` exits 0 with the full suite green (all Phase 1-5 tests plus the new docs/UI guards)."
    - "`uv run ruff check .` exits 0 with 'All checks passed!'."
    - "tests/test_config.py confirms the core demo requires only OPENROUTER_API_KEY (REQUIRED_ENV_VARS = (OPENROUTER_API_KEY,)) and Langfuse is optional; the live NiceGUI launch check is explicitly deferred to /gsd-verify-work."
  artifacts:
    - "(none — verification-only plan; no files modified)"
  key_links:
    - "DOC-06 -> uv run pytest (pyproject.toml testpaths/pythonpath)"
    - "DOC-07 -> uv run ruff check . (pyproject.toml [tool.ruff])"
    - "DOC-08 -> tests/test_config.py + src/openrouter_demo/config.py REQUIRED_ENV_VARS"
  prohibitions:
    - "Must not modify any source, docs, or test file in this plan; it only verifies."
    - "Must not run live OpenRouter calls in automated verification (DOC-08 live check is manual, deferred to /gsd-verify-work)."
---

<objective>
Confirm the phase's three quality gates without changing any file: the full test suite passes (DOC-06), Ruff reports no lint failures (DOC-07), and the code-side evidence shows the core demo runs with only `OPENROUTER_API_KEY` (DOC-08).

**As a** reviewer verifying the finished repo, **I want to** run the two quality-gate commands and see them pass, and confirm from the config tests that a single API key is sufficient to run the demo, **so that** I can trust the repository state before the final live walkthrough.

Purpose: Turn DOC-06/DOC-07/DOC-08 from "already green" into "re-verified after the docs and guard changes landed," and record the one manual-only item for the conversational UAT step.
Output: No file changes; a `06-03-SUMMARY.md` recording the green gate output and the DOC-08 live-check deferral.
</objective>

## Artifacts this phase produces

This plan modifies no files. It verifies the artifacts produced by Plans 06-01 and 06-02:

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `README.md` | modified (rewrite) | Plan 06-01 Task 2 |
| `docs/architecture.md` | created | Plan 06-01 Task 1 |
| `docs/failure-tree.md` | created (moved) | Plan 06-01 Task 3 |
| `docs/specs/failure-tree.md` | deleted (moved) | Plan 06-01 Task 3 |
| `docs/specs/quickstart.md` | modified | Plan 06-01 Task 3 |
| `tests/test_docs.py` | created + extended | Plan 06-01 + Plan 06-02 Task 2 |
| `tests/test_ui.py` | modified (DOC-04 guard) | Plan 06-02 Task 1 |

## Flagged Assumptions (no SPEC/CONTEXT for this phase)

- **DOC-08 live check is manual:** launching the full NiceGUI server requires a real OpenRouter API key and blocks; there is no network in CI. The code-side evidence (`tests/test_config.py` exercising `load_config` for every credential combination) satisfies the automated half; the live launch is deferred to `/gsd-verify-work`. Assumed — RESEARCH "Current State Audit" DOC-08 caveat.

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-RESEARCH.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-VALIDATION.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-01-SUMMARY.md
@.planning/phases/06-interview-walkthrough-and-quality-gates/06-02-SUMMARY.md
@pyproject.toml
@tests/test_config.py
@src/openrouter_demo/config.py
@.env.example
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm uv run pytest passes (DOC-06)</name>
  <files>(none — verification only)</files>
  <read_first>
    - pyproject.toml ([tool.pytest.ini_options] testpaths = ["tests"], pythonpath = ["src"])
  </read_first>
  <action>
    Run the full test suite with `uv run pytest -q`. Confirm the exit code is 0 and the reported count includes the new tests/test_docs.py and tests/test_ui.py guards added in Plans 06-01 and 06-02. If any test fails, do NOT modify any file in this plan (this is a verification-only plan): record the failing test name and its assertion output in 06-03-SUMMARY.md and escalate back to the planner (or to the 06-01/06-02 plan that introduced the regression) instead of applying an in-place fix.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0.
    - Output reports 0 failed (the observed baseline was 100 passed; the new guards increase the count, never decrease it).
  </acceptance_criteria>
  <done>The full test suite is green, including the docs and UI guards added this phase.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm uv run ruff check . passes (DOC-07)</name>
  <files>(none — verification only)</files>
  <read_first>
    - pyproject.toml ([tool.ruff] target-version = "py312", line-length = 100)
  </read_first>
  <action>
    Run `uv run ruff check .` on the repository root. Confirm it exits 0 and reports 'All checks passed!'. The docs and test files added this phase must be lint-clean; if Ruff flags a newly added test file, do NOT modify any file in this plan (this is a verification-only plan): record the flagged file and rule in 06-03-SUMMARY.md and escalate back to the planner instead of applying an in-place fix.
  </action>
  <verify>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check .` exits 0.
    - Output includes the line 'All checks passed!'.
  </acceptance_criteria>
  <done>Ruff reports no lint failures across the repo, including the new tests/test_docs.py.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm core demo runs with only OPENROUTER_API_KEY (DOC-08)</name>
  <files>(none — verification only)</files>
  <read_first>
    - src/openrouter_demo/config.py (REQUIRED_ENV_VARS = (OPENROUTER_API_KEY,) and LANGFUSE_ENV_VARS at lines 10-11; load_config at lines 24-33)
    - tests/test_config.py (test_openrouter_key_sets_required_ready, test_langfuse_ready_requires_all_optional_vars, test_missing_openrouter_key_is_reported)
    - .env.example (the four empty-assignment vars)
    - app.py (main() -> load_config() -> SQLiteRunHistory -> build_app -> ui.run; /health endpoint at lines 22-28)
  </read_first>
  <action>
    Confirm the code-side evidence for DOC-08 by running `uv run pytest tests/test_config.py -q`. This proves: `REQUIRED_ENV_VARS` contains only OPENROUTER_API_KEY; `openrouter_ready` is True with only that key set; `langfuse_ready` is False without the three Langfuse vars; and the app launches with tracing visibly disabled. Do not launch the live NiceGUI server in this task (it requires a real OpenRouter key and blocks). Record in 06-03-SUMMARY.md that the live launch check — run `uv run python app.py` with only OPENROUTER_API_KEY set and confirm the app loads and a live run streams — is deferred to `/gsd-verify-work` as the manual-only DOC-08 verification.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py -q` reports all passed, 0 failed.
    - `grep -c "REQUIRED_ENV_VARS = (OPENROUTER_API_KEY,)" src/openrouter_demo/config.py` returns >= 1.
    - `grep -c "LANGFUSE_ENV_VARS = (LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_BASE_URL)" src/openrouter_demo/config.py` returns >= 1.
    - 06-03-SUMMARY.md records that the DOC-08 live NiceGUI check is deferred to /gsd-verify-work.
  </acceptance_criteria>
  <done>DOC-08 code-side evidence is confirmed via tests/test_config.py, and the manual live check is explicitly deferred to /gsd-verify-work in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README.md -> git history | Human-written docs must never contain real API keys |
| docs -> reviewer | Reviewer-facing docs must not overstate implemented behavior |
| client.py -> Langfuse trace input | Trace input must never contain the API key |
| client.py -> OpenRouter | Live inference spend must stay bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure | README.md / docs/* / git | high | mitigate | `.env.example` keeps empty assignments only; never document real keys; guard tests assert trace input contains no API key |
| T-06-02 | DoS (self-inflicted) | eval prompts / live inference | medium | mitigate | small bounded default prompts + 5 checked-in eval cases + `request_timeout=60.0` |
| T-06-03 | Information disclosure | telemetry.py trace input | high | mitigate | existing `tests/test_evals.py` / `tests/test_telemetry.py` assert no API key in trace input |
| T-06-04 | Availability | telemetry.py `record_trace` | medium | mitigate | `record_trace` catches all exceptions, returns `failed`, never raises |

This phase installs zero new packages, so no package-legitimacy (T-06-SC) threat applies.
</threat_model>

<verification>
- `uv run pytest -q` green.
- `uv run ruff check .` clean.
- `uv run pytest tests/test_config.py -q` green.
</verification>

<success_criteria>
- DOC-06: full suite passes.
- DOC-07: Ruff clean.
- DOC-08: single-credential readiness confirmed via tests; live check deferred to /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/06-interview-walkthrough-and-quality-gates/06-03-SUMMARY.md` when done
</output>
